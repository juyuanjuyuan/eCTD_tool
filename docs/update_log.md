# 进度更新日志

> 每次代码修改必须在此记录。格式: 日期 | 修改内容 | 影响模块 | 关联计划

## 更新记录

| 日期 | 修改内容 | 影响模块 | 关联计划 |
|------|---------|---------|---------|
| 2026-04-24 | **docs(plan): 新增「发布前冷安装验证检查表」（Release Gate）**：在 `docs/plans/software_upgrade.md` §1.0.1 记录首次客户冷装暴露的 6 条 P0 阻塞项（redis YAML 折叠 bug / prisma migrate 未自动执行 / seed 未自动执行 / reference 目录未分发 / 镜像仅 amd64 / backend NOAUTH redis 鉴权），并新增 §1.9 包含 A 源码侧 / B 打包侧 / C 冷装测试侧 / D 客户侧四段串联 Release Gate（共 8+6+10+4=28 项），以及 E 已知坑对照表（7 条）。定位：`build-mac.sh` 出包前必须通过全部检查项，否则不得发客户。检查表强调"干净 Mac"冷装测试的严格定义（不能在构建机/开发机/已装过某版的机器上测），以避免同类问题再次到客户那里才发现。 | docs/plans/software_upgrade.md | software_upgrade（Release Gate） |
| 2026-04-24 | **fix(desktop-compose): 修复 redis 在空 REDIS_PASSWORD 下启动失败**：原 `docker-compose.desktop.yml` 将 `redis-server --requirepass ${REDIS_PASSWORD:-}` 直接写入折叠字符串命令，当 `.env` 里 `REDIS_PASSWORD=` 为空时 YAML 折叠后出现连续空格，shell 将下一个 flag `--appendonly` 误当成 `--requirepass` 的值，redis 启动异常 → healthcheck 永挂 → backend 因 `depends_on: redis(healthy)` 无法启动（用户首次在 Apple Silicon Mac 上双击 `Start.command` 复现此故障，报错 `dependency failed to start: container ectd-desktop-redis is unhealthy`）。改为 `sh -c` 条件分支：`REDIS_PASSWORD` 非空时走 `--requirepass`，空值时退化为 `redis-server --appendonly yes --appendfsync everysec`；healthcheck 同步改为 `CMD-SHELL` 条件版本。兼容客户保留默认空密码与自行设置密码两种模式。 | docker-compose.desktop.yml | software_upgrade（M4 故障修复） |
| 2026-04-24 | **方案 B 二次完善（交付可下载/可测试链路）**：`scripts/build-mac.sh` 新增 `--public-key` 参数，打包时可直接携带 `public.pem`；`.env.template` 的 `DATA_DIR` 改为带引号路径（修复 mac 路径空格导致 source 解析问题）；`Start.command` 新增首次启动自动加载 `images/*.tar.gz`（`docker load`）并写入加载标记；新增 `tools/runtime/get-machine-id.js` 供客户机采集机器码；`docs/deployment.md` 第 10 节补充“如何下载（scp）+ 如何测试（机器码采集→签发激活码→启动）”步骤；`docs/plans/software_upgrade.md` 同步补充 M4 细节。 | scripts/build-mac.sh, tools/runtime/get-machine-id.js, docs/deployment.md, docs/plans/software_upgrade.md | software_upgrade（M4 强化） |
| 2026-04-24 | **software_upgrade（方案 B）继续执行：可下载测试包 + 激活码工具链落地**。新增 `scripts/build-mac.sh`（支持生成 `eCTDTool-mac-vX.Y.Z.tar.gz`，有 `create-dmg` 时可继续产出 `.dmg`；无 Docker 环境可 `--skip-docker` 先产结构包）；新增 `tools/keygen.sh`（RSA-2048 密钥对生成）；新增 `tools/issue-license/issue-license.js`（签发激活码：`Base64Url(payload).Base64Url(signature)`）；新增 `tools/runtime/verify-license.js`（启动前校验签名/机器码/到期，支持 `LICENSE_MACHINE_ID_OVERRIDE` 测试覆盖）；同步更新 `docs/plans/software_upgrade.md` 进度（C8/C9、M4 标记已落地）与 `docs/deployment.md` 第 10 节（方案 B 打包/签发/启动步骤）。 | scripts/build-mac.sh, tools/keygen.sh, tools/issue-license/issue-license.js, tools/runtime/verify-license.js, docs/plans/software_upgrade.md, docs/deployment.md | software_upgrade（C8/C9/M4） |
| 2026-04-24 | **software_upgrade（Mac 线）启动执行 + 云端分支修正说明**：新增 `docker-compose.desktop.yml` 作为桌面单机版首版编排（前端默认端口 `18080`、后端 `13000`，`DATA_DIR` 外置持久化，MinIO 控制台关闭 `MINIO_BROWSER=off`，后端/前端改为镜像运行模式供打包加载）；在 `docs/plans/software_upgrade.md` 增加“1.0 当前执行进度”并标记 M1 已完成，同时补充“1.7 Cloud 平台分支设置修正（master → main）”；在 `docs/deployment.md` 新增云平台默认分支说明，明确若日志出现 `git fetch origin --depth=100 master`，需在平台 Base Branch/Default Branch 改为 `main` 后重部署。 | docker-compose.desktop.yml, docs/plans/software_upgrade.md, docs/deployment.md | software_upgrade（Mac M1） |
| 2026-04-24 | **fix(docker): 修正 backend CMD 入口路径 `dist/main.js` → `dist/src/main.js`**: prod 重 build 后 backend 容器启动报 `Cannot find module '/app/dist/main.js'`。根因：`prisma/seed.ts` 与 `src/main.ts` 被 TypeScript 一起编译，tsc 推断的 `rootDir` 是项目根而非 `src/`，导致 `src/main.ts` 落到 `dist/src/main.js`、`prisma/seed.ts` 落到 `dist/prisma/seed.js`（seed 命令此前已按此路径运行）；但 Dockerfile 的 CMD 仍指向旧的 `dist/main.js`。改 `backend/Dockerfile` CMD 为 `["node", "dist/src/main.js"]`。**无 TypeScript 配置变更**（保留 prisma 与 src 同编译，以便不装 ts-node 直接 `node dist/prisma/seed.js`）。 | backend/Dockerfile | 生产部署修复 |
| 2026-04-24 | **fix(prisma): 声明 binaryTargets 解决 prod 运行时引擎不匹配**: prod 服务器启动 backend 时 Prisma Client 报 `debian-openssl-1.1.x` ↔ `debian-openssl-3.0.x` 不匹配（`node:20-slim` 当前默认带 OpenSSL 3.0，但 Prisma v6 在 builder 阶段的默认 `native` 只产出了 1.1.x 引擎）。在 `backend/prisma/schema.prisma` 的 `generator client` 里显式声明 `binaryTargets = ["native", "debian-openssl-3.0.x"]`，让 `npx prisma generate` 同时产出两个平台引擎，镜像重 build 后 `node dist/main.js` 与 `node dist/prisma/seed.js` 均可直接运行，无需启动时临时 regenerate。**无数据库迁移**，仅 generator 配置变更。**生效方式**: 拉取后在 prod 跑 `docker compose -f docker-compose.prod.yml build backend` 重 build 即可。 | backend/prisma/schema.prisma | 生产部署修复 |
| 2026-04-24 | **Plan 13 全量实施 — 多实例节点 + Completeness 规则 Schema 扩展**: 继 Phase A (4.3.12-4.3.21 九条规则) 之后落地 Plan 13 两大 WP。**(WP-13.2 Schema 扩展)**: migration `20260424034022_plan_13_multi_instance_and_rule_scoping` 新增 `CtdCompletenessRule.sequenceTypeCodes TEXT[]` (默认 `['cnsqt1']`, 仅首次提交触发, 避免 cnsqt2 回复/cnsqt3 撤回误报) + `.productTypeCodes TEXT[]` (默认 `[]` = 适用所有); `CtdTemplateNode.isRepeatable BOOLEAN` + `.instanceKeyFields JSONB?` (DTD 可重复元素 5 个 — `m2-3-s-drug-substance` / `m2-3-p-drug-product` / `m2-7-3-summary-of-clinical-efficacy` / `m3-2-s-drug-substance` / `m3-2-p-drug-product`); `SequenceNode.instanceIndex INT` (默认 0, 非可重复恒 0; 可重复节点及其整个后代子树共享同一值) + `.instanceLabel VARCHAR(200)?` (UI 展示用). 5 个 repeatable 节点元数据通过 SQL 手工回填现有库 (ts-node seed 脚本与 Prisma v6 兼容性问题已知, 已在 seed-ctd.ts 逻辑中对已 seed 库追加 backfill 代码供新库使用). **(WP-13.1 多实例服务层)**: (1) `CtdTemplateService` 新增 3 个方法 `listInstances` / `addInstance` / `removeInstance` — addInstance 按 instanceKeyFields 校验至少填写一项 + 唯一性校验 (同组键组合不能重复) + 深拷贝整个模板子树生成新 SequenceNode (共享 newInstanceIndex) + 首次序列叶子自动 operation=NEW / 非首次也 NEW (符合 eCTD "新增原料药=新增行为"); removeInstance 首次序列物理删除整个子树 (先清 parentId 避免 fk 冲突再 deleteMany), 非首次级联给子树所有叶子打 operation=DELETE (符合 eCTD 生命周期不允许物理删除规范), 强制保留最后一个实例. (2) `CtdTemplateController` 新增 3 个 REST 端点 `GET/POST /sequences/:seqId/template-nodes/:templateNodeId/instances` + `DELETE /sequences/:seqId/instances/:instanceRootNodeId`. (3) `getSequenceNodeTree` 现 include `templateNode: {id, isRepeatable, instanceKeyFields}` 供前端渲染多实例 UI. (4) 新增 DTO `AddInstanceDto` (5 个 optional 骨架属性字段). **(Validator 改造)**: validator.service.ts 的 `validateCompleteness` 按 sequenceType + productType 精筛规则 + 同 templateNode 聚合多实例节点数组 (而非取第一个), 叶节点任一实例 COMPLETED 即满足 REQUIRED, FORBIDDEN 任一实例非空即违规; 新增 `isSectionSatisfied` 辅助函数递归判断 SECTION 容器是否"至少一个后代叶子非空" (用于 `cn-1-11` 三选一子节点等场景). 同样的筛选逻辑在 `ctd-template.service.ts` 的 `checkCompleteness` 和 `previewRequiredSections` 里复制一份. 兼容 mock 缺 `templateNode.isLeaf` 的历史测试: `rule.templateNode.isLeaf \|\| seqNodes.some(n => n.isLeaf)`. **(文件路径隔离)**: `file-name-normalizer.service.ts` `buildEctdRelativePath` 新增第 3 个可选参 `instanceIndex`, `> 0` 时给 folder 最后一段追加 `-N` 后缀 (如 `m3/32-body-of-data/32s-drug-sub-1/spec.pdf`), `=0` 保持历史路径不变; `file.service.ts` 3 处调用点传入 `node.instanceIndex ?? 0`. **(前端 UI)**: (1) `services/ctd.ts` 新增 `listInstances/addInstance/removeInstance` 3 个 API. (2) `types/index.ts` `CtdTemplateNode` 加 `isRepeatable`+`instanceKeyFields`, `SequenceNode` 加 `instanceIndex`+`instanceLabel`+`templateNode?.{id,isRepeatable,instanceKeyFields}`. (3) 新建 `components/AddInstanceModal.tsx` 根据 instanceKeyFields 动态渲染表单输入 (substance→"原料药"/manufacturer→"生产商"/productName→"产品名称"/dosageForm→"剂型"/indication→"适应症"). (4) `components/CTDTree.tsx` 加 props `onRequestAddInstance/onRequestRemoveInstance`, 右键 repeatable 容器节点 (如 3.2.S) 弹添加, 右键实例节点 (有 instanceLabel) 弹删除; 节点标题右侧渲染蓝色 instanceLabel 小标签. (5) `pages/editor/EditorPage.tsx` 接线 AddInstanceModal 弹窗 state + `handleRequestAddInstance`/`handleRequestRemoveInstance` 回调 (removeInstance 通过 antd Modal.confirm 带 danger 确认). **测试结果**: backend `ctd-template + validator + file-name-normalizer` 4 个 suite 153/153 通过; backend + frontend tsc 0 错误. **文档同步**: `database_design.md` §2.4 三张表加字段, `backend_architecture.md` §4.5 后追加"多实例节点 (Plan 13)"章节 + 5 个 repeatable 元素表 + API 清单 + 文件路径规则, `api_design.md` §8 CTD 目录结构新增 3 行端点, `frontend_architecture.md` §2.1 CTDTree 描述扩展 + 新增 §2.1.1 AddInstanceModal, `plan_13_20260423_update.md` WP-13.1/13.2 全部任务标记 [x]. | backend/prisma/schema.prisma, backend/prisma/migrations/20260424034022_plan_13_multi_instance_and_rule_scoping/, backend/prisma/seed-ctd.ts, backend/src/ctd-template/ctd-template.service.ts, backend/src/ctd-template/ctd-template.controller.ts, backend/src/ctd-template/dto/add-instance.dto.ts, backend/src/ctd-template/dto/index.ts, backend/src/ectd/services/validator.service.ts, backend/src/file/file-name-normalizer.service.ts, backend/src/file/file.service.ts, frontend/src/services/ctd.ts, frontend/src/types/index.ts, frontend/src/components/AddInstanceModal.tsx, frontend/src/components/CTDTree.tsx, frontend/src/pages/editor/EditorPage.tsx, docs/architecture/database_design.md, docs/architecture/backend_architecture.md, docs/architecture/api_design.md, docs/architecture/frontend_architecture.md, docs/plans/plan_13_20260423_update.md | plan_13 (WP-13.1+WP-13.2 完成) |
| 2026-04-24 | **CTD 目录对齐 Excel 权威表 + 补充申请 completeness 规则 Phase A**: 根据客户反馈对照 `reference/现行申报资料要求与eCTD目录元素、CTD目录层级对应表.xlsx`（4 个 sheet：按生物制品管理的体外诊断试剂/化学药品一致性评价/药品补充申请/境外生产药品再注册）校对模块 2-5 目录结构 — **结论：当前模板结构与 Excel 权威表完全一致**，Excel 引用的所有 `2.x/3.x/4.x/5.x` 章节号在 ICH XML 中均有对应 element（缺失 0 项），Excel 未引用的节点均为"中间分类节点"属正常；仅发现 Excel 在 2.3 处把 element name 笔误为 `m2-3-introduction`（应为 `m2-3-quality-overall-summary`，`m2-3-introduction` 实为 2.3.1），当前模板正确无需改。**Phase A 落地**：在 `backend/prisma/seed-ctd.ts` 的 `COMPLETENESS_RULES` 末尾新增 9 条规则 4.3.12–4.3.16 + 4.3.18–4.3.21（跳 4.3.17 因 cnapt2/3+cnrat5 为非法组合），覆盖**补充申请 cnrat2**（4.3.12/13：NDA/ANDA/原料药通用必填+禁止未上市专用章节；4.3.14：IND 补充申请不强制 cn-1-4-1）、**备案 cnrat3**（4.3.15）、**报告 cnrat4**（4.3.16）、**再注册 cnrat8**（4.3.18/19 NDA+禁止临床试验专用；4.3.20 ANDA；4.3.21 原料药不强制说明书/包装标签）四大场景；覆盖率从 11/23 合法 apt×rat 组合 → 20/23。所有新组合经 `reference/eCTD技术规范V1.1附件包/附件1-2：受控词汇文件包/depend-apt-rat-sqt.xml` 合法性校验；每条规则代码注释给出 Excel sheet 溯源与设计原则（M1 行政硬性项 ERROR REQUIRED、M2–M5 章节因补充申请覆盖面过广不做 REQUIRED 避免误判）。**同步更新** `docs/architecture/backend_architecture.md` §4.7 验证规则表。**Phase B 拆为独立计划** `docs/plans/plan_13_20260423_update.md` — 覆盖 schema 扩展（sequenceTypeCodes/productTypeCodes 字段 + validator 章节容器语义）+ **多实例节点支持**（客户反馈：同一药品可能有多个原料药，DTD `ich-ectd-3-2.dtd` 定义 `m3-2-s-drug-substance*` / `m3-2-p-drug-product*` / `m3-2-s-2-1-manufacturer` 均可重复，当前 `SequenceNode` 跟 `CtdTemplateNode` 是一对一不支持）。 | backend/prisma/seed-ctd.ts, docs/architecture/backend_architecture.md, docs/plans/plan_13_20260423_update.md, docs/update_log.md | 客户反馈（CTD 对齐） + plan_13(新建) |
| 2026-04-24 | **(1) 编辑器左侧 CTD 目录滚动修复 + (2) 每文件自定义 eCTD 导出名**: (1) `EditorPage.tsx` 左右两个 `<Sider>` 的内部 `.ant-layout-sider-children` 默认不是 flex 容器，导致之前写在内部 div 上的 `flex:1; overflow:auto` 失效，CTD 目录展开超过视窗后下方被截断无法滚动。新建 `frontend/src/pages/editor/EditorPage.css`，写 `.editor-sider .ant-layout-sider-children { display:flex; flex-direction:column; height:100%; min-height:0 }`，把两个 Sider 加上 `className="editor-sider"`，Sider 自身 style 去掉冗余的 `display:flex` + `flexDirection`（由 antd Sider 根节点自身承担）。(2) 用户上传带中文名的文件会被 `normalizeFileName()` 归一为占位名（`产品说明书.pdf` → `file.pdf`），导出后缺少可读性。新增每个 `FileAttachment` 用户自定义的导出 basename：(a) **Schema** — `FileAttachment.exportName String? @db.VarChar(64)`，migration `20260424023630_file_export_name`；(b) **服务** — `file.service.ts` 新增 `updateExportName(nodeId, fileId, exportName)`，校验 `^[a-z0-9\-_]+$`、basename+ext ≤64 字符（ICH §3.3.2）、引用文件（isReference）不允许改名、同节点内按"有效文件名 = exportName ? `${exportName}${fileType}` : storedName"做唯一性检查（eCTD 规范只要求同目录下不重名，同节点=同目录）；通过时更新 `exportName` 与 `ectdRelativePath`（`xlink:href` / ZIP 路径的权威源，`index-xml.service.ts` + `package-assembler.service.ts` 无需改动），`storagePath` 不变保持 MinIO 存储稳定；传空串/null 清除 override 回落到 `storedName`；(c) **DTO/Controller** — `UpdateExportNameDto` + `PATCH /api/v1/nodes/:nodeId/files/:id/export-name`；(d) **前端** — `services/file.ts` 的 `FileAttachment` 增 `exportName: string \| null`，新增 `updateExportName()`；`FilePanel.tsx` 在"文件"列与"合规"列之间加"导出名"列，用 antd `<Text editable>` 内联编辑，显示当前有效 basename（自定义黑色、回落灰色提示）+ 右侧只读扩展名 `.pdf`；引用文件显示"引用文件不可改名"。**类型检查**: backend + frontend `tsc --noEmit` 均 0 错误 | backend/prisma/schema.prisma, backend/prisma/migrations/20260424023630_file_export_name/, backend/src/file/file.service.ts, backend/src/file/file.controller.ts, backend/src/file/dto/index.ts, frontend/src/services/file.ts, frontend/src/components/FilePanel.tsx, frontend/src/pages/editor/EditorPage.tsx, frontend/src/pages/editor/EditorPage.css | UX修复 + 用户需求 |
| 2026-04-09 | **文件大小上限 200MB → 500MB（对齐 ICH eCTD Submission Formats v1.2 §2.3）**: 用户决策从原 NMPA V1.1 推荐的 200MB 上调到 ICH 上限 500MB（XPT 4GB 例外保持不变）。**注意**：plan_4 历史记录显示该值原本是 ICH 500MB，V1.1 当时主动下调为 200MB；本次回调到 ICH 上限是用户的明确决策，技术上 ICH 用 "should not"（推荐性）措辞，并未禁止 200-500MB 之间的取值。改动覆盖：(1) **后端常量** `file-name-normalizer.service.ts:11` `MAX_FILE_SIZE = 500*1024*1024` + 错误标签 `'500MB'`; (2) **后端校验** `validator.service.ts:296` rule 2.2 magic number 200→500 + detail label，加注释指向 ICH 出处; (3) **PDF 合规** `pdf-compliance.service.ts:33` `MAX_FILE_SIZE_MB = 500` + 注释更新; (4) **前端拦截** `FilePanel.tsx:189` maxSize + `message.error` 标签 + 上传区 hint 文案; (5) **Nginx** `nginx.conf` `client_max_body_size` 200M→500M（共 3 处：根 server / api proxy location / HTTPS 注释模板）; (6) **测试 fixture 同步**：`file-name-normalizer.service.spec.ts` 5 处（边界值 200→500、超限 201→501、文案）+ `validator.service.spec.ts` 1 处（201→501）+ `ectd-compliance.spec.ts` 3 处（边界 + 检测 detail + XPT 测试用 1GB 替代原 500MB 以避免与新限混淆）；(7) **架构文档**：`backend_architecture.md` 验证规则表 + 错误码 1015 描述 / `frontend_architecture.md` 文件大小校验描述 / `user-guide.md` §6 错误码表 2.2 + §3.x 验证规则描述 / `skills/file_storage.md` multer 配置示例。**历史 plan 文件不动**（plan_4/5/6/8 都是当时的事实记录）。**测试结果**：file-name-normalizer + validator.service + ectd-compliance + pdf-compliance 共 4 个 suite **189/189 通过**；frontend tsc **0 错误**；backend tsc **0 错误**。**附带修复**：user-guide.md §6 表里 2.5 路径限制 180→230（plan 11 的合规修复漏写到用户文档，本次顺手补） | backend/src/file/file-name-normalizer.service.ts(+spec), backend/src/ectd/services/validator.service.ts(+spec), backend/src/ectd/services/ectd-compliance.spec.ts, backend/src/export/pdf-compliance.service.ts, frontend/src/components/FilePanel.tsx, frontend/nginx.conf, docs/skills/file_storage.md, docs/architecture/backend_architecture.md, docs/architecture/frontend_architecture.md, docs/user-guide.md | 用户决策（合规对齐 ICH） |
| 2026-04-09 | **Plan 12 全量完成（P1/P2/P3/P4 汇总 + P5 文档同步）** — STF v2 多研究模型端到端上线。**(P1 Schema 迁移)**: 新建 `20260408120000_stf_v2_schema` migration，`DROP TABLE study_tagging_file CASCADE`（丢弃 v1 单表 1:1 设计）+ 创建三张新表 `study` / `study_category` / `study_document`（一个 sequence_node 可挂 N 份 Study，每份 Study 可挂 N 个 category 维度 + N 份 PDF 文档）；`study` 表含 `operation` + `modified_from_id` 生命周期列与 `stf_xml_content`/`stf_checksum` 缓存列，`@@unique([sequenceNodeId, studyId])`；`ctd_template_node` 新增 `default_stf_categories JSONB` 字段（支撑决策 4 — 章节预设维度）。跟进 migration `20260408120100_widen_cv_code_for_stf` 把 `controlled_vocabulary.code` 从 `VARCHAR(10)` 放宽到 `VARCHAR(80)`（ICH STF file-tag 值如 `inter-laboratory-standardisation-methods-quality-assurance` 可达 58 字符）。**(P2 CV 种子 + 纯 XML Service + 章节默认维度 seed)**: `controlled-vocabulary.service.ts` 新增 `seedStfVocabularies()` / `getStfCategories()` / `getStfCategoryValues(name)` / `getStfFileTags(module: 'm4'\|'m5')`，`onModuleInit` 幂等种子 ICH STF v6.0 (Nov 2023) valid-values.xml，realm 以 `[ich]/[us]/...` 前缀编码在 `descriptionEn`，`vocabularyName` 键: `stf-category-{species,route-of-admin,duration,type-of-control}` + `stf-file-tag-{m4,m5}`；`controlled-vocabulary.controller.ts` 暴露 `GET /api/v1/cv/stf/categories`、`/stf/categories/:name`、`/stf/file-tags?module=m4\|m5` 三个端点；新建 `backend/src/ectd/services/study-tagging-file.service.ts` 纯 XML in/out service（`generateStfXml` / `parseStfXml` / `computeStfChecksum` / `validateStructure`，零 DB / 零 FS 访问，DTD 相对路径 `../../../util/dtd/ich-stf-v2-2.dtd`）；新建 `backend/prisma/seeds/stf-default-categories.ts` 定义 ~30 个 M4/M5 叶章节的 `STF_DEFAULTS` 映射表（4.2.1.x→species+route-of-admin；4.2.2.x→+duration；4.2.3.x→全 4 维；M5 5.3.1/3/4/5→route-of-admin/type-of-control 等），`applyStfDefaultCategories()` 在 `seed-ctd.ts` 里幂等执行，按 "宁缺勿滥" 原则将无明确 ICH 维度的章节留 null（风险点 #7）。**(P3 Backend Service 集成)**: 新建 `backend/src/study/study.module.ts` 装配 `StudyService` + `StudyTaggingFileImportService` + `StudyController`，依赖 `ControlledVocabularyModule` / `EctdModule` / `FileModule`，在 `app.module.ts` 注册；`study.service.ts` 提供 Study/StudyCategory/StudyDocument 级联 CRUD，按 CV 表校验 category name/value/file-tag（`cvService.getStfCategories` + `getStfFileTags(module)`），每次 save 在事务内重算 `stfXmlContent` + `stfChecksum`，REPLACE/APPEND/DELETE 自动 application-scoped 降序查找前序 Study 解析 `modifiedFromId`，找不到抛 `BadRequestException`；`study.controller.ts` 暴露 9 个 REST 接口（见下方 API 文档）；`index-xml.service.ts` 引入 `buildStfLeaves` + `deriveStfPath`，STF 必需章节（M4 4.2.x + M5 5.3.1-5.3.5）的 leaf 指向 `m{N}/.../study-<slug>.xml` 而非直接指向 PDF，PDF 由 STF 内 `<doc-content>` 引用；`package-assembler.service.ts` 同目录写入 STF XML 文件（决策 2 锁定），命名 `study-<slug>.xml`，`assemblePackage` / `assemblePackageStream` / `previewStructure` 三条路径均已更新，STF 条目使用 `study.stfChecksum` 写入 `index-md5.txt`，v1 `getStfPath`（原 `<basePath>/<subFolder>/stf-<section>.xml`）已删除；`validator.service.ts` 第 5 类 STF 规则重写为 v2 版，规则编号保留 CDE V1.1 原编（5.1/5.4/5.5/5.6/5.8/5.10/5.11/5.12/5.13/5.14/5.16/5.18/5.20），全部遍历 `node.studies[]` 支持多研究，规则 5.7（研究标题必须匹配 leaf 标题）因 v2 一个 leaf 可承载多研究而废除。v1 文件整体删除: `backend/src/ectd/services/stf.service.ts` + spec + `ectd.controller.ts` 的 v1 STF 端点 + `SaveStfDto` + `schema.prisma` 的 `StudyTaggingFile` model。**(P4 Backend 导入 + Frontend UI)**: 后端 `study-tagging-file-import.service.ts` 提供 `importStfXml`（粘贴模式）+ `importStfBundle`（XML + 附带 PDF 模式），3 种冲突策略 `reject`/`overwrite`/`merge`，事务内解析+校验+写 FileAttachment+创建 Study，软降级处理未找到的 `modifiedFromHref` / 缺失 PDF / MD5 不一致（参见 2026-04-09 上一条记录的详细描述）；前端新建 `services/study.ts` REST client 覆盖 CRUD+导入接口，`services/cv.ts` 添加 `getStfCategories/getStfCategoryValues/getStfFileTags`，`services/ectd.ts` 删除 v1 STF 端点；新建 `components/StudyMetadataPanel.tsx`（Plan 12 §4.1 — 基础区按 `defaultStfCategories` 渲染必填维度 Select + 高级区 Collapse 折叠允许追加自定义维度 + 文档选择器 + regenerate-xml 按钮），新建 `components/StudyImportModal.tsx`（Plan 12 §4.3 — 粘贴 XML Tab + 上传 bundle Tab via Dragger + 冲突模式选择 + warning 列表展示）；`pages/editor/PropertiesPanel.tsx` 新增 "研究 (STF)" Tab，仅在 `isStfSection(ctdSectionNumber)` 为真的 STF 必需叶节点显示。**(P5 文档同步 — 本条)**: 更新 `docs/architecture/database_design.md` (§2.7 重写为 v2 三表 + `ctd_template_node.default_stf_categories` + `controlled_vocabulary.code` VARCHAR(80) 说明)、`backend_architecture.md` (§1.1 新增 `study` 模块 + §4.6 重写 STF 服务段落，拆分 StudyTaggingFileService/StudyService/StudyTaggingFileImportService + ControlledVocabularyService STF 方法 + index-xml/package-assembler 集成说明)、`api_design.md` (删除 §12 `/nodes/:nodeId/stf` 与 §12b `/stf/categories\|file-tags` 的 v1 端点、§7 受控词汇新增 3 个 STF 条目、新增 §12 "研究与 STF" 章节覆盖 9 个 v2 Study 端点)、`frontend_architecture.md` (新增 §2.10 `StudyMetadataPanel` + §2.11 `StudyImportModal` + §4 新增 `services/study.ts` / cv STF 补充说明)、`plan_12_stf.md` (§1.3/2.3/3.5/3.6/4.4/5 所有任务标记 `[x]`)。**状态**: Plan 12 全部 5 个 phase 完成（含 P5 测试集成 + 文档同步），STF v2 multi-study 模型可用于向 CDE 真实申报 | backend/prisma/migrations/20260408120000_stf_v2_schema/, backend/prisma/migrations/20260408120100_widen_cv_code_for_stf/, backend/prisma/schema.prisma, backend/prisma/seeds/stf-default-categories.ts, backend/prisma/seed-ctd.ts, backend/src/study/*, backend/src/ectd/services/study-tagging-file.service.ts, backend/src/ectd/services/index-xml.service.ts, backend/src/ectd/services/package-assembler.service.ts, backend/src/ectd/services/validator.service.ts, backend/src/ectd/ectd.controller.ts, backend/src/ectd/ectd.module.ts, backend/src/controlled-vocabulary/controlled-vocabulary.service.ts, backend/src/controlled-vocabulary/controlled-vocabulary.controller.ts, backend/src/app.module.ts, frontend/src/services/study.ts, frontend/src/services/cv.ts, frontend/src/services/ectd.ts, frontend/src/components/StudyMetadataPanel.tsx, frontend/src/components/StudyImportModal.tsx, frontend/src/pages/editor/PropertiesPanel.tsx, docs/architecture/database_design.md, docs/architecture/backend_architecture.md, docs/architecture/api_design.md, docs/architecture/frontend_architecture.md, docs/plans/plan_12_stf.md, docs/update_log.md | plan_12 全部阶段完成 |
| 2026-04-09 | **Plan 12 阶段 3.6 — STF 导入服务（P4 后端分片）**: 新建独立的 `StudyTaggingFileImportService`（与 v2 CRUD 的 `StudyService` 分离，per plan §3.6 锁定决策）反向把 STF XML 写回 Study/StudyCategory/StudyDocument 三张表，为 ERIS 迁移 / 跨团队协作 / STF 校验底座服务。**(1) 解析复用**: 只复用 `StudyTaggingFileService.parseStfXml()`，不写第二个解析器；先做 studyId/title 非空校验→加载 sequenceNode（含 requiresStf 守卫）→从 CV 表查 category names + 每 category 的合法 values + m4/m5 file-tags，未知值分类用 BadRequestException.getResponse() 返回结构化的 `{ message, unknownCategories / invalidCategoryValues / unknownFileTags }` 便于前端列表渲染。**(2) 两种导入模式**: `importStfXml(sequenceNodeId, xml, options)` 只接 XML 字符串，每个 `document.href` 去当前 sequenceNode 下查 basename 匹配的既存 FileAttachment，没找到 → 软降级为 warning 并丢弃该文档（Study 仍创建）；`importStfBundle(sequenceNodeId, { xmlString, attachedFiles, onConflict })` 额外接一组 `{ originalName, buffer, md5? }`，bundle 模式先查 attachedFiles，命中则实际计算 buffer MD5 并与 XML 中 checksum 比对（不匹配 → warning + 使用实际 MD5），上传 MinIO（`@Optional() @Inject(MinioService)`，无 Minio 时降级为只写元数据），创建新 FileAttachment 行；落回既存 FileAttachment 查找逻辑作为兜底。**(3) 生命周期**: 任何 parsed doc 带 `modified-file` 属性时触发 `resolveModifiedFromStudy()` — 按 applicationId + templateNodeId + sequenceNumber `lt` 倒序查前序 Study 取第一条；找不到 → warning + `modifiedFromId=null`，Study 仍创建（plan §3.6 锁定为"降级为 WARNING，导入成功但 lifecycle 链断裂"）。Study.operation 从每个 doc-content 的 `operation` 属性聚合：全部一致取该值，混合 → warning + 回退 NEW。**(4) 冲突处理**: `onConflict='reject'`（默认）同 `(sequenceNodeId, studyId)` 已存在 → ConflictException；`'overwrite'` 删旧 Study（级联 children）+ 新建新 id；`'merge'` 保留旧 Study.id + createdAt，deleteMany children 后重插 categories/documents，重算 stfXmlContent + stfChecksum。**(5) 事务**: 整个 create/merge/overwrite + FileAttachment 创建 + XML 重生成包进一个 `$transaction`，任何一步失败整体回滚。**(6) REST 接口** (扩展 `study.controller.ts`，同现有 CRUD Guards + Roles.ADMIN/MANAGER/EDITOR): `POST /api/v1/sequence-nodes/:nodeId/studies/import-xml` JSON body；`POST /sequence-nodes/:nodeId/studies/import-bundle` JSON body（base64 files 兼容转不了 multipart 的客户端）；`POST /sequence-nodes/:nodeId/studies/import-bundle-multipart` 用 `FileFieldsInterceptor([{name:'xml'},{name:'files',maxCount:50}])` 真正的 multipart，limits=512MB/file。**(7) Module 装配**: `StudyModule` imports 追加 `FileModule`（拿 MinioService），providers/exports 追加 `StudyTaggingFileImportService`。**(8) DTO**: 新建 `dto/import-study.dto.ts` 定义 `ImportStfXmlDto` / `ImportStfBundleDto` / `ImportAttachedFileDto` + `ImportOnConflict` 联合类型，index.ts 用 `export type` 单独导出类型以通过 `isolatedModules`。**(9) 测试**: 新建 `study-tagging-file-import.service.spec.ts` 共 14 个用例（超过 plan 要求的 ≥10），自建 `FakePrisma` 覆盖 sequenceNode/fileAttachment/study/studyCategory/studyDocument 的 findUnique/findFirst/findMany/create/update/delete/deleteMany + 一个带快照回滚的 `$transaction`；覆盖成功导入 / studyId 空拒绝 / 未知 category / 未知 file-tag / reject 冲突 / overwrite 冲突 / merge 冲突 / modifiedFromHref 解析成功 / modifiedFromHref 无前序 warning / 文件未匹配 warning / bundle MD5 不一致 warning / bundle 新建 FileAttachment / NotFoundException / requiresStf=false 拒绝。**测试结果**: `npx jest --testPathPatterns='study'` 2 suite 48/48 通过（14 个新 + 34 个原 study-tagging-file.service.spec）；`npx tsc --noEmit -p tsconfig.json` 退出码 0。**未触动**: validator.service / package-assembler.service / index-xml.service / 任何 frontend 文件（P4 前端并行 agent 负责）；study.service.ts / controlled-vocabulary.service.ts / schema.prisma（已锁定不改）。 | backend/src/study/study-tagging-file-import.service.ts, backend/src/study/study-tagging-file-import.service.spec.ts, backend/src/study/study.module.ts, backend/src/study/study.controller.ts, backend/src/study/dto/import-study.dto.ts, backend/src/study/dto/index.ts | plan_12 (阶段 3.6 P4 后端分片) |
| 2026-04-08 | **Plan 12 阶段 1 — STF 受控词汇导入（P2 分片）**: 解析 `reference/eCTD技术规范V1.1附件包/附件2-6：STF标签值文件/valid-values.xml`（ICH STF v6.0 / November 2023 基线）写入 `ControlledVocabulary` 表，与 cnapt/cnprt/cnrat/cnsqt 同表不同 `vocabularyName` 维度。**(1) 命名规范**: 4 个 category 键 `stf-category-species` / `stf-category-route-of-admin` / `stf-category-duration` / `stf-category-type-of-control`；file-tag 键 `stf-file-tag-m4` / `stf-file-tag-m5`（因 valid-values.xml 的 `<file-tag>` 块是扁平共享的、不按模块拆分，所以每个 ICH/US/JP file-tag 会同时写入 m4 和 m5 两条记录，便于按模块直查）。**(2) realm 编码**: CV 表无 realm 列，约定 `descriptionEn = "[<realm>] <value>"`（如 `[ich] mouse`、`[us] short`），`descriptionZh` 存裸值作展示回退，查询 API 通过正则 `^\[(\w+)\] (.*)$` 反解。**(3) 实现**: 在 `controlled-vocabulary.service.ts` 新增 `seedStfVocabularies()` + `parseStfValidValuesFile()`（私有方法，未抽到 `prisma/seeds/` 是因为现有 `prisma/seed.ts` 不引用 seeds 目录），注入 `onModuleInit()` 二次调用（幂等：`vocabularyName LIKE 'stf-%'` 存在则跳过并日志 "STF CV already seeded"）；`version=6.0`、`validFrom=2023-11-01` 硬编码为 v6.0 冻结基线（fast-xml-parser 默认丢注释，版本号只在 XML 注释头里）。**(4) 查询 API**: 新增 `getStfCategories()` / `getStfCategoryValues(name)` / `getStfFileTags(module)` 3 个方法（均走 Redis cache 24h），Controller 新增 `GET /api/v1/cv/stf/categories` / `/stf/categories/:name` / `/stf/file-tags?module=m4\|m5`（module 参数做 `BadRequestException` 白名单校验）。**(5) 测试**: `controlled-vocabulary.service.spec.ts` 新增 8 条 STF 测试（seed 幂等 / 解析出 4 category + 每 category 条数 + m4/m5 file-tag 各 97 条 / realm 编码 / getStfCategories 分组 + cache / getStfFileTags 按模块 cache key / getStfCategoryValues 单类），共 28 测试全部通过；controller.spec 4 测试未受影响。**(6) 约束**: 未动 schema.prisma（`code` 目前 VarChar(10)，部分 STF 值（如 `foreign-clinical-studies-not-under-ind` 40 字符）在运行时 seed 会触发列宽不足，需配套迁移，本分片不处理）；未动 validator / package-assembler / index-xml / study-tagging-file 等并行 P1/P3 文件范围。**tsc 通过**（`npx tsc --noEmit -p tsconfig.json` 退出码 0）。 | backend/src/controlled-vocabulary/controlled-vocabulary.service.ts, backend/src/controlled-vocabulary/controlled-vocabulary.controller.ts, backend/src/controlled-vocabulary/controlled-vocabulary.service.spec.ts, docs/plans/plan_12_stf.md | plan_12 (阶段1 P2 分片) |
| 2026-04-08 | **测试代码清理 (31 个 tsc 错误 + 2 个失败测试 + 1 个 TS2554)**: 全部为 spec 文件遗留问题，不涉及生产代码。**(1) supertest e2e 类型**: `test/app.e2e-spec.ts` 13 个 TS2349 错误，单一根因 — 旧写法 `import * as request from 'supertest'` 与新版 supertest 类型不兼容，改为 `import request from 'supertest'`（默认导入，依赖 `esModuleInterop:true`），13 个错误一次性消除；**(2) Nullable result 断言**: auth.controller / auth.service / edit-lock.controller / ectd.controller 共 5 处，统一加 `expect(result).not.toBeNull(); result!.xxx` 模式；**(3) Mock 与生产 type 不匹配**: approval.controller (`status` → `approvalStatus`)、ctd-template.controller (`count` → `nodeCount`、`isComplete` 改为读 `missingRequired.length === 0 && completedRequired === N`)、ectd.controller (`category` → `categories`，`{conflicts:[]}` → 直接 `[]`)，全部跟随生产代码真实 type 改 mock，未触动生产代码；**(4) ctd-template.service.spec.ts** 5 处 implicit any: `cache.get<T>()` 默认 `T={}` 导致 `getTemplateTree/getTemplateTreeWithRules` 推断为 `Promise<any[]|{}>`，spec 端用 `as any[]` 断言绕过（生产代码不动，作为 finding 留给后续清理）；**(5) document.service.spec.ts:171** 回调参数 `({ data }: { data: any })` 加类型标注；**(6) package-assembler.service.spec.ts** TS2554 第 152 行：构造器实际签名 5 参（prisma/cnRegionalXml/indexXml/md5Service/minioService），spec 多塞了一个 `validator` mock，删掉多余参数和对应的 `let validator` + mock 块；**(7) package-assembler 2 个失败测试** (`should throw when unapproved required sections exist` / `should throw when validation fails`) 是 2026-03-31 plan_3 主动废弃的硬约束遗留 dead test（当时为 UX 体验移除了导出前必须验证/审批通过的限制），直接删除而非改 not throw。**测试结果**：backend tsc **0 错误**（从 31 → 0）；package-assembler 12/12 通过（14-2 删除）；auth/approval/ctd-template/document/ectd.controller/edit-lock 12 个 suite 142/142 全通过。**Findings 待后续**: (a) `CtdTemplateService.getTemplateTree` 应给 cache.get 加显式泛型 `<any[]>` 或函数返回类型，避免 `{}` 污染；(b) 项目里如果再写 e2e spec 应使用 `import request from 'supertest'` | backend/test/app.e2e-spec.ts, backend/src/auth/(2 spec), backend/src/edit-lock/edit-lock.controller.spec.ts, backend/src/approval/approval.controller.spec.ts, backend/src/ctd-template/(2 spec), backend/src/document/document.service.spec.ts, backend/src/ectd/ectd.controller.spec.ts, backend/src/ectd/services/package-assembler.service.spec.ts | 测试代码清理 |
| 2026-04-08 | **方案 C - sequenceNumber 申请维度全局唯一化（彻底重构）+ ICH 规范合规修复 6 项**: 起因 — 用户上午先要求"前端展示成全局连续"，临时在 ApplicationDetailPage 加了一个 useMemo 投影；下午用户决定按 ICH eCTD v3.2.2 + NMPA V1.1 的规范本意，把后端 sequenceNumber 也改成申请维度全局唯一连续。同时新加 4 份 ICH 规范文件（eCTD_Specification_v3_2_2_0.pdf / Specification_for_Submission_Formats_for_eCTD_v1_2.pdf / STFV2-6-1_0.pdf / E3_Guideline.pdf）到 `reference/实施规范指南/`，并要求合规审计与一并修复。**(A) 方案 C 全栈**: (1) Prisma schema — Sequence 表新增 `applicationId` 字段+反向关系，删除原 `@@unique([regulatoryActivityId, sequenceNumber])`，新增 `@@unique([applicationId, sequenceNumber])` + `@@index([applicationId])`；migration `20260408000000_sequence_global_numbering` 内含 truncate（用户授权丢弃测试数据）+ 加列+加约束；(2) `sequence.service.ts` create() 与 createWithRegulatoryActivity() 改为按 application 维度查询 max sequenceNumber 并写入 applicationId；(3) `lifecycle.service.ts` validateOperation/findPreviousOperation/validateReplaceLanguage 改为 application-scoped；(4) `cn-regional-xml.service.ts` 与 `index-xml.service.ts` 的 buildPriorLeafIdMap 改为跨 RA 查询，去掉旧的单序列 break，**顺便补 leaf 元素 `xlink:type="simple"` 属性（ICH v3.2.2 强制）**；(5) `regulatory-activity.service.ts` create() 的 relatedSequence 改为申请维度 max+1；(6) `validator.service.ts` 4 处生命周期查询（rule 3.11/3.30/3.33、4.1.x、`getLastOperation`、`checkLanguageConsistency`）从 RA-scoped 改为 application-scoped，函数签名 `regulatoryActivityId` → `applicationId`；(7) **前端**: ApplicationDetailPage 拆掉今天上午加的 sortedRaList useMemo 投影，dataIndex 从 `globalSequenceNumber` 改回 `sequenceNumber`，分组排序按"该分组首条 sequenceNumber 升序"，nextSeqHint 改为读真实 max；(8) 测试 fixture 修复：`lifecycle.service.spec.ts`、`index-xml.service.spec.ts`、`ectd-advanced-scenarios.spec.ts`、`xml-backbone-validation.spec.ts`、`regulatory-activity.service.spec.ts`、`cn-regional-xml.service.spec.ts` 共 ~15 处 mock 加 `regulatoryActivity.applicationId` 字段、relatedSequence 期望从 0003 改为 0004。**(B) 合规修复**: (B1) 路径长度上限 `MAX_PATH_LENGTH` 180→230（ICH eCTD v3.2.2 §2.4），`file-name-normalizer.service.ts:7` + 同步更新 validator rule 2.5 + 3 个相关 spec 期望；(B2) `validator.service.ts` 新增规则 **4.3.M5** — 模块 5 章节完整性校验：当 sequence 含模块 5 内容时，必须存在 5.3.5.1 / 5.3.5.2 / 5.3.5.3 节点，缺失 emit ERROR（基于 ICH E3）；(B3) **申请编号格式校验** `^[xyls]\d{4}\d{5}$`（NMPA V1.1）：`create-application.dto.ts` 加 `@Matches` + `APPLICATION_NUMBER_REGEX` 常量，`application.service.ts` create() 加服务层兜底校验（覆盖 caller-supplied 和 auto-gen 两条路径）；(B4) 3.2.R 扩展节点（NMPA V1.1）：`ctd-template.service.ts` 加 `EXTENSION_NODE_ALLOWED_PRODUCT_TYPE='cnprt2'` 常量替代魔字符串，加 doc 注释说明 6 个子节点定义来源于 `node-extension-property_CN.xml`（保留 NMPA 中文标题，不改为 ICH M4Q 标题）；(B5) **PDF 字体嵌入校验**（eCTD Submission Formats v1.2）：`pdf-compliance.service.ts` 新增规则 **6.25 ERROR**（字体未嵌入）+ **6.26 WARNING**（字体子类型异常），重写 `checkFontCompliance` 走 pdf-lib 低层 API 遍历 Page→Resources→Font→DescendantFonts→FontDescriptor，处理 Type0 复合字体、subset 前缀去除、Standard 14 例外，safeCheck 包装失败降级为 6.25-SKIP；(B6) `index-xml.service.ts` 同步补全 `checksum-type="md5"` / `xlink:type="simple"` 强制属性。**STF 实现拆为独立 Plan 12（`docs/plans/plan_12_stf.md`）**，本轮不做。**测试结果**：方案 C 相关 9 个 spec suite 共 214 测试全部通过；validator + ectd-compliance + ectd-advanced-scenarios + xml-backbone-validation 共 4 个 suite 189 测试全部通过；pdf-compliance 17 测试通过（含 3 个新增）；application/ctd-template/file-name-normalizer 共 3 个 suite 99 测试通过（含 9 个新增）。frontend tsc 0 错误；backend tsc 31 个 pre-existing 错误（auth/document/superagent e2e 等不相关文件，不在本次改动文件内） | backend/prisma/schema.prisma, backend/prisma/migrations/20260408000000_sequence_global_numbering/, backend/src/sequence/sequence.service.ts, backend/src/ectd/services/lifecycle.service.ts, backend/src/ectd/services/cn-regional-xml.service.ts, backend/src/ectd/services/index-xml.service.ts, backend/src/ectd/services/validator.service.ts, backend/src/regulatory-activity/regulatory-activity.service.ts, backend/src/file/file-name-normalizer.service.ts, backend/src/application/dto/create-application.dto.ts, backend/src/application/application.service.ts, backend/src/ctd-template/ctd-template.service.ts, backend/src/export/pdf-compliance.service.ts, frontend/src/pages/application/ApplicationDetailPage.tsx, 多个 spec 文件 | 方案C + plan_11 后续合规修复 + plan_12 (STF 拆出) |
| 2026-04-07 | **导航/UX 优化 (5项)**: (1) **ProjectListPage** — 项目列表每行末尾新增「操作」列，蓝色 primary 「进入项目」按钮（带 ArrowRightOutlined 图标）替代项目名称链接，作为唯一显眼入口; (2) **ProjectDetailPage** — 申请表格 appColumns 末尾新增「操作」列，蓝色 primary 「进入申请」按钮（含 useNavigate hook 跳转 `/projects/:id/applications/:appId`），项目名称恢复为普通文本; (3) **ApplicationDetailPage** — 序列行操作里的「编辑」按钮文本改为「填入文件」、type 由 link 改为 primary（更显眼），跳转逻辑保留指向 `/sequences/:seqId`; (4) **CTDTree** — 关闭 antd Tree 的 `virtual` 模式与固定 `height` 计算，移除 ResizeObserver/treeHeight/containerRef，让目录树自然撑高，由 EditorPage Sider 内已有的 `overflow:auto` 容器负责上下滚动，解决展开模块后下方模块被遮挡的问题; (5) **FilePanel** — 在 `Upload.Dragger` 上传区文案下方新增红色加粗提示「请将文件改成英文名称」，提醒用户遵守 eCTD 文件命名规范（仅 a-z/0-9/-/_） | frontend/src/pages/project/ProjectListPage.tsx, frontend/src/pages/project/ProjectDetailPage.tsx, frontend/src/pages/application/ApplicationDetailPage.tsx, frontend/src/components/CTDTree.tsx, frontend/src/components/FilePanel.tsx | UX优化 |
| 2026-04-01 | **大文件上传修复 (>50MB无响应问题)**: 根因: (1) 前端从不使用分片上传，整个文件单次 multipart 发送; (2) 后端 Multer MemoryStorage 将整个文件加载到内存; (3) 分片上传也全内存拼接(Buffer.concat)。修复: (1) **frontend/src/services/file.ts** — 文件>10MB 自动切换分片上传(5MB/片)，逐片发送+整体进度跟踪+5分钟超时; (2) **backend/src/file/file.service.ts** — 分片写入磁盘临时文件(os.tmpdir)而非内存Map，完成后流式上传MinIO，新增废弃上传清理(30分钟超时自动清理); (3) **backend/src/file/minio.service.ts** — 新增 uploadFileStream() 方法，PassThrough 流式计算 MD5+上传，避免全文件缓冲; (4) **frontend/vite.config.ts** — 开发代理增加 timeout/proxyTimeout 600s 配置 | frontend/src/services/file.ts, backend/src/file/file.service.ts, backend/src/file/minio.service.ts, frontend/vite.config.ts | 文件上传 |
| 2026-03-31 | **eCTD 导出 BUG 修复 (2项)**: (1) cn-regional.xml 中 xlink:href 路径错误修复 — 从 `m1/cn/00/file.pdf`(相对序列根）改为 `00/file.pdf`（相对 cn-regional.xml 自身位置 m1/cn/），cn-regional-xml.service.ts buildLeafAttributes 方法中 strip `m1/cn/` 前缀; (2) index-md5.txt 修复 — cn-regional.xml 路径从 `cn-regional.xml` 改为 `m1/cn/cn-regional.xml`（相对序列根），新增所有内容文件的 MD5 条目; md5.service.ts generateIndexMd5 扩展支持预计算 MD5; package-assembler.service.ts 两个 assemble 方法同步修复 | backend/src/ectd/services/cn-regional-xml.service.ts, backend/src/ectd/services/md5.service.ts, backend/src/ectd/services/package-assembler.service.ts | eCTD合规 |
| 2026-03-31 | **产品体验优化 (UI布局调整)**: (1) 完整性/验证/XML预览/eCTD导出 从编辑器右侧面板移至中央区域上方Tab栏（编辑/内容完整性/eCTD验证/XML骨架预览/eCTD导出），右侧面板恢复为纯属性面板(360px)；(2) eCTD导出去掉验证前置要求 — 前端EctdPackagePanel移除validationPassed限制和警告提示，后端package-assembler.service.ts两个方法(assemblePackage/assemblePackageStream)移除验证检查和审批检查，允许用户自由导出模块拼接 | frontend/src/pages/editor/EditorPage.tsx, frontend/src/components/EctdPackagePanel.tsx, backend/src/ectd/services/package-assembler.service.ts | UX优化 |
| 2026-03-31 | **产品体验优化 (5项)**: (1) 新建项目后自动跳转到项目详情页(ProjectListPage handleCreate → navigate); (2) 项目详情页合并"概览"和"申请"Tab — 申请列表嵌入概览Tab; (3) 合并"创建注册行为"与"创建序列"为一步操作 — 后端新增 POST /applications/:appId/create-sequence 合并API(SequenceService.createWithRegulatoryActivity 事务内 find-or-create RA + create sequence)，前端 ApplicationDetailPage 改为单个"创建序列"按钮+级联表单; (4) 序列页(/sequences/:seqId)直接变成编辑器界面 — 移除 SequenceDetailPage 中间页，编辑器右侧面板集成完整性/验证/XML预览/eCTD导出4个Tab，编辑器内处理CTD目录初始化; (5) CTDTree 目录树添加颜色标识(红=必填未填, 蓝=编辑中, 绿=已完成)和图例说明 | frontend/src/pages/project/ProjectListPage.tsx, frontend/src/pages/project/ProjectDetailPage.tsx, frontend/src/pages/application/ApplicationDetailPage.tsx, frontend/src/pages/editor/EditorPage.tsx, frontend/src/components/CTDTree.tsx, frontend/src/App.tsx, frontend/src/services/application.ts, backend/src/sequence/(dto+service), backend/src/application/(controller+module) | UX优化 |
| 2026-03-27 | **全局功能缺陷修复 (Plan 11, 14 项修复)**: **P0**: (1) FIX-01 MinIO presigned URL 修复 — minio.service.ts 新增 MINIO_PUBLIC_URL 配置，toPublicUrl() 替换内部地址为外网地址，.env 新增 MINIO_PUBLIC_URL; (2) FIX-02 指派功能修复 — 新建 dto/create-assignment.dto.ts 替代 interface，修复 ValidationPipe whitelist 清空问题; 放宽权限检查从 OWNER-only 到 VIEWER-excluded; PropertiesPanel 改用 useAuthStore 获取用户; (3) FIX-03 PDF 合规检查修复 — pdf-compliance.service.ts 移除所有静默 catch，新增 safeCheck 包装器在检查失败时生成 WARNING; 加密检测改为扫描完整文件; **P1**: (4) FIX-04 状态更新 UI 刷新 — EditorPage refreshNodes 后查找并更新 selectedNode; (5) FIX-05 骨架属性 Input 添加 key={node.id} 强制重新挂载; (6) FIX-06 eCTD 包导出 Blob 修复 — ectd.ts exportPackage 改用独立 axios 实例，EctdPackagePanel 移除 double-wrap; (7) FIX-07 语言属性 Select 添加 onChange 处理器; (8) FIX-08 APPROVED/SUBMITTED 节点状态 Select 禁用; **P2**: (9) FIX-09 导出按钮无叶节点时 disabled; (10) FIX-10 ValidationPanel 加载报告后通知父组件 validationPassed; (11) FIX-12 后端 updateSequenceNode 新增状态转换校验; (12) FIX-13 Dashboard/SequenceDetail 跳转编辑器传 nodeId 参数; EditorPage 读取 URL nodeId 自动选中; (13) FIX-14 SequenceDetailPage 返回按钮 history fallback | backend/src/file/minio.service.ts, backend/.env, backend/src/assignment/, backend/src/export/pdf-compliance.service.ts, backend/src/ctd-template/ctd-template.service.ts, frontend/src/pages/editor/, frontend/src/components/, frontend/src/services/ectd.ts, frontend/src/pages/dashboard/, frontend/src/pages/sequence/ | plan_11 |
| 2026-03-27 | **WP-09 架构文档更新**: 同步更新 4 份架构文档以反映 Plan 9 实施内容。(1) backend_architecture.md — 核心模块表新增 assignment/notification/collaboration/dashboard 4 个模块，模块依赖关系新增 5 行，错误码新增 1019-1022，新增 §7 协作模块详细设计(7.1 InvitationController 含邀请/接受/角色变更/所有权转移, 7.2 AssignmentModule 含 assignNode/checkNodePermission 权限继承5级规则, 7.3 NotificationModule 含 10 种通知类型和 9 个触发点, 7.4 CollaborationGateway 含 Socket.IO/JWT/房间/9 个 WebSocket 事件, 7.5 DashboardModule 含 4 个聚合查询); (2) database_design.md — ER 关系新增 3 行，§2.10 新增 3 张表(project_invitation/node_assignment/notification)，索引新增 7 行(含 2 个唯一约束)，枚举新增 InvitationStatus/NodePermission/NotificationType，ActionType 扩展 6 个新值; (3) api_design.md — 新增 §20-27 共 8 个 API 分组(项目邀请/成员角色/章节指派/通知/在线状态/工作台/项目协作/成员活动)，共计 17 个新 API 端点; (4) frontend_architecture.md — 页面清单更新 3 行(dashboard/project-detail/notifications)，新增 §6 协作功能组件(6.1 NotificationCenter/6.2 NotificationListPage/6.3 DashboardPage 增强/6.4 ProjectDetailPage 增强含协作 Tab+成员管理+在线头像/6.5 PropertiesPanel 指派 Tab/6.6 useCollaboration Hook) | docs/architecture/ | plan_9 |
| 2026-03-27 | **WP-09 多成员协同编辑与项目协作增强 — 全 6 阶段实施**: (1) **阶段1 成员管理增强** — Prisma Schema 新增 3 个枚举(InvitationStatus/NodePermission/NotificationType) + 3 个模型(ProjectInvitation/NodeAssignment/Notification)，迁移 `20260327094209_add_invitation_assignment_notification`; ProjectService 新增 createInvitation(邮箱邀请，已注册直接添加+通知，未注册生成token链接7天过期)/listInvitations/cancelInvitation/acceptInvitation(验证邮箱匹配+过期检查)/changeMemberRole(OWNER专属+不可设OWNER)/transferOwnership(事务降级+提升+全员通知); removeMember 增强(编辑锁检查+待审批检查); ProjectController+InvitationController 新增 7 个 API; 前端 ProjectDetailPage 全面增强(邀请弹窗+角色变更下拉+移除确认+所有权转移弹窗+邀请记录列表); (2) **阶段2 细粒度权限与章节指派** — AssignmentModule(assignNode 批量upsert+通知/getNodeAssignments/removeAssignment/getSequenceAssignmentOverview/checkNodePermission 权限继承链 OWNER全权限→VIEWER仅VIEW→显式覆盖→父链继承→MEMBER默认EDIT); EditLockController 注入 AssignmentService 获取锁前检查 EDIT 权限; 前端 assignment API + PropertiesPanel 新增"指派"Tab(搜索用户+权限选择+指派列表+取消指派); (3) **阶段4 站内通知系统** — NotificationModule(create/createMany/findAll 分页+筛选/markAsRead/markAllAsRead/getUnreadCount); NotificationController 4 个 API; 前端 NotificationCenter 组件(Popover下拉+未读角标+30s轮询+已读/未读样式+点击跳转); NotificationListPage(/notifications 全量列表+类型筛选+状态筛选); BasicLayout 顶栏集成铃铛图标; (4) **阶段3 WebSocket实时协同** — 安装 @nestjs/websockets+@nestjs/platform-socket.io+socket.io; CollaborationGateway(/ws/collaboration 命名空间, JWT鉴权, join:project/join:sequence 房间, user:online/offline/location 事件, heartbeat 60s续期, emitToSequence/emitToProject/emitToUser 服务端推送); CollaborationService(getProjectPresence Redis SCAN); RedisCacheService 新增 scanKeys; 前端 socket.io-client + useCollaboration Hook(自动连接/重连/心跳/在线用户列表/位置更新); (5) **阶段5 工作台与协作看板** — DashboardModule(getMyTasks 待编辑+待审阅+待处理邀请/getRecentEdits 最近编辑去重/getProjectProgress 按模块统计/getProjectWorkload 成员工作量含编辑锁统计); DashboardController 4 个 API; 前端 DashboardPage 全面重做(4统计卡片+待编辑列表+最近编辑+待审阅+我的项目); ProjectDetailPage 新增"协作"Tab(进度总览表+模块进度条+成员工作量分布表); (6) **阶段6 审计增强** — ActivityLogService 新增 logMemberAction + getMemberActivity(项目维度成员活动时间线); ActivityLogController 新增 GET projects/:projectId/members/:userId/activity; ProjectService 关键操作(INVITE/INVITE_ACCEPT/ROLE_CHANGE/OWNERSHIP_TRANSFER)写入审计日志; 前端 activityLogApi 新增 getMemberActivity | backend/prisma/, backend/src/project/, backend/src/assignment/, backend/src/notification/, backend/src/collaboration/, backend/src/dashboard/, backend/src/edit-lock/, backend/src/activity-log/, backend/src/common/, backend/src/app.module.ts, frontend/src/types/, frontend/src/services/, frontend/src/pages/, frontend/src/layouts/, frontend/src/components/, frontend/src/hooks/ | plan_9 |
| 2026-03-26 | **WP-09 计划文档创建**: 新增 `docs/plans/plan_9.md` — 多成员协同编辑与项目协作增强计划。6 个阶段: (1) 成员管理增强(邮箱邀请机制+角色变更+所有权转移); (2) 细粒度权限与章节指派(node_assignment 表+权限继承+编辑拦截+指派总览); (3) 实时协同感知(WebSocket+Socket.IO+在线状态+编辑锁广播+内容变更广播); (4) 站内通知系统(notification 表+10 种通知类型+WebSocket 实时推送+通知中心 UI); (5) 成员工作台与进度仪表盘(个人待办+最近编辑+项目协作看板+成员工作量); (6) 成员操作审计增强(新 action 类型+成员活动时间线)。涉及 3 张新表(project_invitation/node_assignment/notification)、5 个后端新模块、7 个前端新页面/组件。 | docs/plans/ | plan_9 |
| 2026-03-20 | 项目初始化：创建文档结构、CLAUDE.md、架构文档、技术 Skills、执行计划 | 全部 | 准备阶段 |
| 2026-03-20 | 重写全部 8 份执行计划（plan_1~plan_8），从偏技术实现转向以 eCTD 法规合规为核心。深度整合: 受控词汇体系、信封元素不可变性、生命周期状态机、撤回序列4步流程、骨架属性更新规则、STF生成、内容完整性规则(4.3.x)、V1.1升级的8项PDF错误级别规则、电子签章要求、文件命名/路径规范、扩展节点限制等。同步更新 CLAUDE.md 核心业务逻辑和术语表。 | 全部计划文档、CLAUDE.md | 准备阶段 |
| 2026-03-20 | 全面更新架构文档，与重写后的执行计划对齐: (1) database_design.md — 新增 regulatory_activity/controlled_vocabulary/cv_dependency/ctd_completeness_rule/file_pdf_analysis/study_tagging_file/comment/activity_log 等表，受控词汇改用 VARCHAR 代码; (2) backend_architecture.md — 新增 controlled-vocabulary/regulatory-activity 模块，信封元素管理表，生命周期状态机，错误码扩至 1001-1018; (3) api_design.md — 新增注册行为/受控词汇/STF/编辑锁/审批/评论/日志 API; (4) frontend_architecture.md — CTD 树增加 STF/电子签章/合规状态图标，编辑器增加中文字体规范/语言标记，新增 useCVStore; (5) tech_stack.md — 后端目录新增 regulatory-activity/controlled-vocabulary/completeness/pdf-compliance/envelope/package-assembler/file-name-normalizer; (6) CLAUDE.md — 计划描述更新为重写后的标题 | 全部架构文档、CLAUDE.md、tech_stack.md | 准备阶段 |
| 2026-03-21 | **WP-01 全面实施**: (1) Docker Compose — PostgreSQL 16 + Redis 7 + MinIO; (2) NestJS 后端 — Prisma schema(8张表: user/project/project_member/application/regulatory_activity/sequence/controlled_vocabulary/cv_dependency), PrismaModule, 公共模块(TransformInterceptor/AllExceptionsFilter/PaginationDto/JwtAuthGuard/RolesGuard/Roles装饰器); (3) 受控词汇模块 — XML 解析服务自动解析 4 个 cv-*.xml 和 depend-apt-rat-sqt.xml，4 个级联查询 API; (4) 认证模块 — JWT 注册/登录/刷新/me, bcrypt 密码加密, UserModule CRUD; (5) 项目管理 — CRUD + 成员管理 + 用户过滤; (6) 申请管理 — 自动生成申请编号(x/y/s前缀), 信封属性不可变, CV 依赖验证; (7) 注册行为管理 — depend-apt-rat-sqt 三级关联校验, related-sequence 自动计算; (8) 序列管理 — 自动递增序列号, 不允许跳号, 序列类型校验, 状态管理; (9) React 前端 — Vite + Ant Design + Zustand, 登录/注册页, ProtectedRoute, Axios Token 拦截器, 项目列表/详情/申请管理/注册行为/序列管理前端页面, 受控词汇级联选择; (10) 种子数据 — admin/manager/editor 测试用户 | backend/, frontend/, docker-compose.yml | plan_1 |
| 2026-03-21 | **WP-01 验证完成**: (1) Prisma 首次迁移成功 (`20260321034135_init`，8 张表全部创建); (2) 种子数据导入 3 个测试用户; (3) 受控词汇 XML 解析验证通过（4+2+9+4 条记录 + 完整依赖矩阵）; (4) 全链路 API 测试通过: 登录→创建项目→创建申请(自动编号前缀正确)→创建注册行为(依赖校验正确，非法组合 cnapt1+cnrat3 被拒绝)→创建序列(自动递增 0000→0001); (5) Docker 一键启动验证通过; (6) CV 服务路径修复: __dirname 改为 process.cwd() 解决 dist 目录下运行时路径错误 | backend/, docker-compose.yml | plan_1 |
| 2026-03-21 | **WP-02 CTD 目录结构与完整性规则**: (1) Prisma Schema — 新增 5 个枚举(CtdNodeType/SequenceNodeStatus/LeafOperation/CompletenessRuleSeverity/CompletenessRuleType) + 3 个模型(CtdTemplateNode/CtdCompletenessRule/SequenceNode，含自引用树结构和骨架属性字段); (2) 种子脚本 `seed-ctd.ts` — 解析 element-property_CN.xml(71 节点) + element-property_ICH.xml(158 节点)，共 229 节点(5 MODULE/43 SECTION/180 LEAF/1 EXTENSION_POINT)，含 STF/电子签章/扩展节点标记; (3) 完整性规则 — 硬编码 130 条 4.3.1~4.3.11 规则(90 REQUIRED + 40 FORBIDDEN); (4) CtdTemplateModule — 含 Service(模板树/序列初始化/节点管理/骨架属性/扩展节点/完整性检查) + Controller(8 个 API) + 3 个 DTO; (5) 前端 — CTDTree 组件(搜索/状态标签/扩展节点 Modal)、CompletenessPanel 组件(统计卡片/模块进度/缺失列表)、SequenceDetailPage(初始化/目录树/完整性看板 Tab); (6) 全链路验证通过: 创建序列→初始化→229 节点→完整性检查(12 必填章节正确匹配 4.3.1) | backend/prisma/, backend/src/ctd-template/, frontend/src/components/, frontend/src/pages/sequence/ | plan_2 |
| 2026-03-22 | **WP-04 eCTD 合规文档导出**: (1) 安装依赖 — docx(Word导出) + puppeteer(PDF渲染) + pdf-lib(PDF后处理/书签/合规检查) + @nestjs/bull+bull(异步任务队列); (2) WordExportService — TipTap JSON→docx 完整转换(heading/paragraph/bulletList/orderedList/table含合并拆分/blockquote/image占位/horizontalRule/link), A4 2cm页边距, 宋体12pt正文+黑体标题, 1.5倍行距, 页眉页脚页码, 自定义编号样式; (3) PDFExportService — Puppeteer HTML→PDF(eCTD合规CSS: 宋体/黑体/12pt/10.5pt表格/A4/tagged PDF), pdf-lib后处理(添加书签Outline+/Fit目标即Inherit Zoom, 设置PageMode=UseOutlines显示书签面板), stripExternalLinks自动剥除http/https/mailto外部链接; (4) PDFComplianceService — 实现8条错误级别规则(6.18版本1.4-1.7, 6.19加密, 6.20 JavaScript检测Names/AA/OpenAction, 6.21外部URI链接遍历, 6.22多媒体RichMedia/Screen/Sound/Movie/3D, 6.1书签>5页, 6.23书签Zoom检查, 6.24附件/嵌入文件) + 警告级别(文件大小200MB, 字体嵌入检查排除标准14字体); (5) ExportService+Controller — POST word/pdf单章导出(流式下载), POST batch异步(Bull Queue), GET status/:taskId 任务状态; PDF导出失败时返回合规检查JSON; (6) ExportProcessor — Bull Queue @Process word-batch/pdf-batch, 逐节点导出+进度上报+合规检查; (7) BullModule.forRoot Redis连接 + ExportModule注册; (8) 前端ExportModal组件 — 格式选择Word/PDF, eCTD合规提示, 导出进度Spin, 合规结果展示(Summary+错误列表+警告列表+已移除外部链接), Blob下载; EditorPage顶栏新增导出按钮; exportApi服务(word/pdf单章+batch+taskStatus) | backend/src/export/(6文件), frontend/src/components/ExportModal.tsx, frontend/src/services/export.ts, backend/src/app.module.ts | plan_4 |
| 2026-03-22 | **WP-05 eCTD XML 骨架文件、生命周期管理与验证引擎**: (1) Prisma Schema — 新增 2 个枚举(ValidationSeverity/ComplianceStatus) + 5 个模型(FileAttachment/FilePdfAnalysis/StudyTaggingFile/ValidationReport/ValidationItem)，Sequence 新增 validationReports 关联，SequenceNode 新增 fileAttachments/studyTaggingFile 关联; 迁移 `20260322022537_add_ectd_tables`; (2) EctdModule — 7 个服务 + 1 个控制器: CnRegionalXmlService(cn-regional.xml 生成: cn-envelope 12属性 + cn-content 模块一目录元素 + 递归树结构 + 空章节过滤), IndexXmlService(index.xml 生成: DTD/XSL 引用 + 模块二至五目录元素 + 骨架属性substance/manufacturer/product-name/dosageform/indication + node-extension 扩展节点), StfService(STF 生成: ich-stf-v2-2.dtd 格式 + study-identifier + category/file-tag 从 valid-values.xml 解析 + study-document 文件引用), LifecycleService(生命周期状态机: 首序列0000全new + 后续序列状态转换表new→replace/delete + replace→replace/delete + delete→new + modified-file 前序序列验证 + replace语言一致性检查 + 撤回序列4步自动生成 + 并行变更冲突检测), Md5Service(MD5 计算 + index-md5.txt 生成 + 叶元素ID生成N+UUID), ValidatorService(80+条验证规则: 1.x基础识别文件数/大小/空缺统计 + 2.x文件大小200MB/4GB限制/扩展名/命名规范a-z0-9-_/路径180字符/单名64字符 + 3.x ICH骨架操作类型/首序列全new/replace-append-delete需modified-file/前序存在性/delete无href-checksum/路径正斜杠/相对路径 + 4.x区域信息申请编号格式/受控词汇验证/原始编号10位/序列号4位/联系人必填 + 4.3完整性规则REQUIRED/FORBIDDEN + 5.x STF必填/标题/编号/非STF不应有STF/append警告 + 6.x PDF版本1.4-1.7/加密/JS/外部链接/多媒体/书签>5页/Zoom/附件/字体嵌入), PackageAssemblerService(eCTD包组装: 验证前置/目录结构生成{appNumber}/{seqNumber}/m1-m5+util + 从reference复制10个DTD/Schema/XSL/valid-values文件 + cn-regional.xml/index.xml/index-md5.txt + ZIP打包 + 预览结构API); (3) EctdController — 15 个 API: GET xml/cn-regional/index 预览, GET/PUT nodes/:nodeId/stf, GET stf/categories/file-tags, POST validate-operation, GET parallel-conflicts, POST withdraw-preview, POST validate, GET validate/latest/report, GET ectd-preview, POST ectd-package 流式ZIP下载; (4) 前端 — ectdApi 服务(15个API调用), ValidationPanel 组件(运行验证/错误-警告-提示统计/分类筛选/级别筛选/验证明细表格/修复建议), EctdPackagePanel 组件(预览文件结构树/导出ZIP下载/验证未通过阻止导出), XmlPreviewPanel 组件(cn-regional.xml和index.xml在线预览), SequenceDetailPage 新增3个Tab(eCTD验证/XML骨架预览/eCTD导出); (5) 安装 archiver + @types/archiver 依赖 | backend/prisma/, backend/src/ectd/(8文件), frontend/src/services/ectd.ts, frontend/src/components/(3文件), frontend/src/pages/sequence/ | plan_5 |
| 2026-03-22 | **WP-03 富文本编辑器与文档编辑**: (1) Prisma Schema — 新增 Document/DocumentVersion 模型(迁移 `20260322000424_add_document_tables`)，SequenceNode 新增 document 关联; (2) DocumentModule 后端 — DocumentService(获取/保存文档、版本快照/恢复、中文+英文字数统计、xml_lang 语言属性) + DocumentController(6 个 API: GET/PUT 文档、GET 版本列表、GET 指定版本、POST 创建快照、POST 恢复版本) + SaveDocumentDto; (3) TipTap 编辑器 — 安装 13 个 @tiptap 扩展包，RichEditor 组件(StarterKit+Table+Image+Link+Underline+TextAlign+Placeholder+CharacterCount)，Toolbar 组件(标题H1-6/加粗/斜体/下划线/删除线/对齐/列表/引用/表格下拉菜单含行列增删+合并拆分/图片/撤销重做)，editor.css(eCTD 中文规范: 宋体正文+黑体标题+12pt+1.5倍行距+黑色文字+蓝色链接+表格10.5pt); (4) EditorPage 三栏布局 — 左栏CTD目录树(可折叠)/中栏TipTap编辑器/右栏属性面板(可折叠)，顶栏面包屑+保存状态指示器+手动保存按钮; (5) PropertiesPanel — 基本信息Tab(CTD编号/标题/模块/状态选择器/操作类型选择器(首序列0000固定new)/语言属性/字数版本统计/骨架属性输入框)+版本历史Tab(快照创建/版本列表/恢复确认); (6) useAutoSave Hook — 3秒防抖保存+flushSave节点切换时立即保存+beforeunload警告; (7) useEditorStore — Zustand 状态管理(selectedNode/document/isDirty/saveStatus/面板折叠); (8) 路由集成 — `/sequences/:seqId/editor` 全屏编辑器路由 + SequenceDetailPage 新增"进入编辑器"按钮; (9) document API 服务 — documentApi(get/save/getVersions/getVersion/createSnapshot/restore) | backend/prisma/, backend/src/document/, frontend/src/components/RichEditor/, frontend/src/pages/editor/, frontend/src/hooks/, frontend/src/stores/, frontend/src/services/, frontend/src/App.tsx | plan_3 |
| 2026-03-22 | **Plan 1-6 补全实施**: (1) EditorPage 顶栏 — 新增"运行 eCTD 验证"按钮(SafetyCertificateOutlined + ectdApi.runValidation) + 内容完整性状态 Tag("必填 X/Y 已完成"，绿/橙色); (2) 后端 Dockerfile — node:20-slim + Puppeteer 系统依赖(Chromium) + 中文字体(wqy-zenhei/microhei) + Prisma generate + npm run start:dev; (3) 导出下载 API — GET `/export/download/:taskId` 通过 MinIO presigned URL 下载批量导出 ZIP; ExportProcessor 升级: word-batch/pdf-batch 完成后将文件打包为 ZIP 上传至 MinIO(exports/前缀); ExportModule 导入 FileModule 获取 MinioService; (4) 后续序列继承 — initializeSequenceNodes 增加前序序列状态继承: 查找同一注册行为下最新前序序列，继承叶节点 status(EMPTY/EDITING/COMPLETED)，复制扩展节点(node-extension); (5) 骨架属性更新规则 3.6 — updateBackboneAttributes 增加 substance/manufacturer 变更检测: 变更时自动遍历子章节叶节点并标记 operation=NEW(重建)，使用 getDescendantIds 递归查找所有后代; (6) 初始化前必填预览 — GET `/sequences/:seqId/preview-required` API 返回必填/禁止章节清单; SequenceDetailPage 初始化面板新增必填章节清单展示(InfoCircleOutlined + 可滚动列表 + ERROR/WARNING Tag); (7) Redis 缓存 — RedisCacheService(ioredis 封装, get/set/del/delPattern, 连接失败优雅降级), 注册为全局服务(PrismaModule); CV 服务 4 个查询 API 全部加缓存(cv:前缀, 24h TTL); 模板树服务 getTemplateTree/getTemplateTreeWithRules 加缓存(ctd:前缀, 24h TTL); (8) Plan 文件更新 — Plan 1: 序列详情页✅/CV缓存✅/Dockerfile✅; Plan 2: 模板API过滤✅/继承✅/骨架规则3.6✅/预览✅/缓存✅; Plan 3: MinIO图片✅/文件引用✅/导出按钮✅/验证按钮✅/完整性状态✅; Plan 4: PDF标记✅/修复建议✅/下载API✅ | backend/src/ctd-template/, backend/src/export/, backend/src/controlled-vocabulary/, backend/src/common/, backend/src/prisma/, backend/Dockerfile, frontend/src/pages/editor/, frontend/src/pages/sequence/, frontend/src/services/ | plan_1,2,3,4 |
| 2026-03-22 | **WP-07 协作、审批流程与电子签章**: (1) Prisma Schema — 新增 ApprovalStatus 枚举(DRAFT/SUBMITTED/APPROVED/REJECTED) + Comment 模型(sequenceNodeId/userId/content/parentId 自引用回复) + ActivityLog 模型(userId/action/resource/resourceId/detail JSONB)，SequenceNode 新增 approvalStatus/submittedBy/submittedAt/approvedBy/approvedAt/rejectionReason 审批字段; 迁移 `20260322060840_add_plan7_collaboration`; (2) EditLockModule — RedisCacheService 扩展(setnx 原子锁+expire TTL续期)，EditLockService(acquireLock SETNX 30min TTL/releaseLock 仅持有者可释放/queryLock 查询/forceUnlock MANAGER+/heartbeat 5min续期, Redis不可用时优雅降级)，EditLockController(5个API: POST lock/DELETE lock/GET lock/DELETE lock/force/POST lock/heartbeat); (3) ApprovalModule — ApprovalService(submitForApproval 状态校验 DRAFT/REJECTED→SUBMITTED/approveNode SUBMITTED→APPROVED/rejectNode SUBMITTED→REJECTED+理由/unlockApproval MANAGER重置为DRAFT/getSequenceApprovalStatus 序列审批总览含必填章节统计/getApprovalHistory 含用户名解析/checkNodeEditable)，ApprovalController+SequenceApprovalController(6个API: POST submit/approve/reject/unlock-approval, GET approval-history, GET sequences/:seqId/approval-status)，RejectApprovalDto; (4) CommentModule — CommentService(createComment 含 parentId 验证同节点/getComments 树形结构组装/deleteComment 作者+MANAGER可删/getCommentCount)，CommentController(3个API: POST/GET/DELETE comments); (5) ActivityLogModule — ActivityLogService(log 通用日志记录/getBySequence 按序列关联节点查询+分页)，ActivityLogController(GET sequences/:seqId/activity-log); (6) 导出审批门控 — PackageAssemblerService.assemblePackage 新增 Step 0: 检查所有 isRequired 叶节点 approvalStatus===APPROVED，未全部通过则阻止导出; DocumentService.saveDocument 新增审批锁定: approvalStatus===APPROVED 时抛出 ForbiddenException; (7) AppModule 注册 4 个新模块; (8) 前端类型 — SequenceNode 新增 approvalStatus/submittedBy/submittedAt/approvedBy/approvedAt/rejectionReason，新增 EditLockInfo/Comment/ActivityLog/ApprovalHistory/SequenceApprovalStatus 接口; (9) 前端 API 服务 — editLockApi(acquire/release/query/forceUnlock/heartbeat)、approvalApi(submit/approve/reject/unlockApproval/getHistory/getSequenceStatus)、commentApi(create/list/delete)、activityLogApi(getBySequence); (10) EditorPage 编辑锁集成 — 选中节点时 acquireLock, 切换/离开时 releaseLock, 5min heartbeat 定时器, 锁冲突时显示"xx正在编辑"Tag+只读模式, 已审批节点 Alert 提示+只读, RichEditor editable={!isReadOnly}, 新增"提交审批"按钮(SendOutlined); (11) CTDTree 审批状态图标 — CheckCircleOutlined 绿色(APPROVED)/ClockCircleOutlined 蓝色(SUBMITTED)/CloseCircleOutlined 红色(REJECTED); (12) PropertiesPanel 新增审批Tab — 审批状态/提交人/时间/审批人/驳回理由, MANAGER 通过/驳回按钮+驳回弹窗, MANAGER 解锁审批按钮; 新增评论Tab — 评论输入框+Ctrl+Enter发送, 回复(parentId), 评论列表(嵌套回复), 删除(作者+MANAGER), Badge 评论数 | backend/prisma/, backend/src/edit-lock/, backend/src/approval/, backend/src/comment/, backend/src/activity-log/, backend/src/common/, backend/src/document/, backend/src/ectd/, backend/src/app.module.ts, frontend/src/types/, frontend/src/services/, frontend/src/pages/editor/, frontend/src/components/CTDTree.tsx | plan_7 |
| 2026-03-22 | **WP-06 eCTD 文件管理、命名规范与版本控制**: (1) 安装依赖 — minio(MinIO SDK) + @types/multer(文件上传类型); (2) FileNameNormalizerService — eCTD 合规文件名转换(a-z/0-9/-/_ only, 大写→小写, 中文/特殊字符→连字符, 连续连字符合并), 路径长度验证(≤180字符/≤64单名), 文件扩展名验证(.pdf/.xml/.xpt/.txt/.xsl), 文件大小验证(200MB/4GB for XPT), eCTD 相对路径构建(M1: m1/cn/00-12, M2-5: ICH子目录映射), MinIO 存储路径构建(projectId/appNumber/seqNumber/ectdPath), 路径分隔符验证(仅正斜杠); (3) MinioService — MinIO 客户端(endpoint/port/accessKey/secretKey from env), Bucket 自动创建(onModuleInit), 文件上传(putObject+MD5计算), 文件下载(getObject→Buffer/Stream), 文件存在性检查(statObject), 文件删除(removeObject), presigned URL 生成(下载1h/预览1h inline); (4) FileService — 文件上传全流程(扩展名验证→大小验证→加载节点上下文→文件名规范化→eCTD路径构建→MinIO上传+MD5→创建FileAttachment记录→PDF自动合规分析→自动更新节点状态EMPTY→EDITING), 批量上传(uploadFiles), 文件列表/详情/删除(含引用关系保护), presigned下载/预览URL, 文件引用创建(验证同申请+非草稿+前序序列号<当前), 可引用文件列表(遍历同申请所有前序序列的非引用文件), 编辑器图片上传(PNG/JPG/GIF/SVG→MinIO editor-images路径, 24h presigned URL); (5) FileController — 10 个 API: POST upload/upload-batch, GET files/files/:id, DELETE files/:id, GET download/preview, POST reference, GET referenceable, POST editor/upload-image; (6) FileModule — 注册 FileService+MinioService+FileNameNormalizerService+PDFComplianceService; (7) PackageAssemblerService 升级 — 注入 MinioService(Optional), buildZip 优先从 MinIO 读取文件(fallback 本地磁盘), 空章节自动跳过(无文件附件的叶节点不生成文件夹), 引用文件不重复打包, previewStructure 同步过滤空章节和引用; (8) EctdModule 更新 — imports FileModule 获取 MinioService 依赖; (9) 前端 fileApi 服务 — upload(FormData+进度回调)/list/detail/delete/download/preview/createReference/listReferenceable/uploadEditorImage; (10) FilePanel 组件 — Upload.Dragger 拖拽上传+前端扩展名/大小校验, Progress 上传进度条, List 文件列表(Badge 合规状态图标绿/黄/红+引用Tag+文件类型图标), 文件操作(预览PDF/下载presigned/删除确认), 合规详情 Modal(errors/warnings Table), 引用管理 Modal(Table 列出前序序列文件+序列号/章节/文件名/大小+引用按钮); (11) PropertiesPanel 新增"文件"Tab(PaperClipOutlined 图标+FilePanel); (12) RichEditor 图片上传改用 MinIO(fileApi.uploadEditorImage 替代 URL.createObjectURL, 支持 sequenceId prop); (13) EditorPage 传递 sequenceId 给 RichEditor | backend/src/file/(6文件), backend/src/ectd/, backend/src/app.module.ts, frontend/src/services/file.ts, frontend/src/components/FilePanel.tsx, frontend/src/components/RichEditor/, frontend/src/pages/editor/ | plan_6 |
| 2026-03-22 | **WP-08 补充测试**: (1) **新增 4 个服务测试套件共 71 tests** — controlled-vocabulary.service.spec.ts(19 tests: getApplicationTypes/getProductTypes/getRegulatoryActivityTypes 含 appType 过滤/getSequenceTypes 含过滤+null过滤/validateDependency 含sqtType/getCvVersion/seedSkip), document.service.spec.ts(16: getDocument 含空文档/saveDocument 含创建更新状态转换EMPTY→EDITING/APPROVED阻止/中英文字数统计/getVersions/getVersion/createSnapshot/restoreVersion 含先保存当前再恢复), pdf-compliance.service.spec.ts(11: PDF 1.4-1.7 通过/2.0+1.3 拒绝/无法解析/加密检测/书签>5页/<=5页/summary/fileSizeMB/isCompliant), word-export.service.spec.ts(18: 空文档/段落/H1-H6/加粗斜体下划线删除线/bullet+ordered列表/嵌套列表/表格含colspan-rowspan/引用/分隔线/图片占位/链接/硬换行/文本对齐/页眉/null内容/未知节点/复合文档); (2) **eCTD 合规测试矩阵 62 tests** — ectd-compliance.spec.ts: 6种申请类型×注册行为组合(cnapt1-4 × cnrat1/2/8)的信封验证+CV校验+文件命名, 无效CV代码检测(appType/productType/ratType/sqtType), 信封元素验证(applicationNumber/productNumber/sequenceNumber/contactInfo/description), 生命周期场景(首次提交0000全NEW/REPLACE+DELETE错误/模块1非NEW错误/后续序列REPLACE前序存在性/DELETE无文件/非DELETE缺文件), 边界测试(文件名大小写特殊字符/路径>180字符/单名>64字符/文件>200MB/XPT 4GB例外/反斜杠/绝对路径), PDF验证(加密/JS/无书签>5页/外部链接/多媒体/版本1.3/附件/字体嵌入警告), STF验证(缺少STF/缺标题/缺编号/非STF有STF/非STF用APPEND), 文件扩展名(非法类型警告/合法类型通过), 报告持久化; **总计 14 套件 326 tests 全部通过** | backend/src/controlled-vocabulary/controlled-vocabulary.service.spec.ts, backend/src/document/document.service.spec.ts, backend/src/export/pdf-compliance.service.spec.ts, backend/src/export/word-export.service.spec.ts, backend/src/ectd/services/ectd-compliance.spec.ts | plan_8 |
| 2026-03-23 | **WP-08 测试覆盖率达标 (80%+)**: 新增 26 个测试套件共 229 tests，后端总计 **635 tests / 44 suites 全部通过**，覆盖率 **Statements 80.62% / Lines 82.01% / Functions 84.9%**。新增测试: (1) 服务测试 — auth.service.spec.ts(9: register/login/refreshToken/getProfile), project.service.spec.ts(12: create+OWNER/findAll+search/findOne/update权限/archive/addMember/removeMember+OWNER保护), comment.service.spec.ts(12: createComment含回复+parentId验证/getComments树形+空/deleteComment作者+ADMIN+MANAGER+权限/getCommentCount), regulatory-activity.service.spec.ts(7: create含relatedSequence/dependency验证/findAll/findOne), user.service.spec.ts(7: findAll含search+role过滤/findOne/update/disable), redis-cache.service.spec.ts(16: get/set/del/delPattern/setnx/expire 含连接/断开/错误场景), export.processor.spec.ts(11: handleWordBatch成功+缺文档+缺节点+导出错误+自定义headerText+MinIO上传+混合结果/handlePdfBatch+合规检查+缺HTML+导出错误+无MinIO); (2) 控制器测试 — ectd.controller.spec.ts(14), export.controller.spec.ts(8), ctd-template.controller.spec.ts(11), approval.controller.spec.ts(6), edit-lock.controller.spec.ts(5), project.controller.spec.ts(7), document.controller.spec.ts(6), sequence.controller.spec.ts(5), application.controller.spec.ts(4), user.controller.spec.ts(4), file.controller.spec.ts(13), auth.controller.spec.ts(4), regulatory-activity.controller.spec.ts(3), comment.controller.spec.ts(3), cv.controller.spec.ts(4); (3) 集成测试 — package-assembler.service.spec.ts(14: assemblePackage含ZIP生成+审批门控+验证门控+序列不存在+DELETE跳过+引用跳过+MinIO失败降级+空章节跳过/previewStructure含排序+空序列+DELETE过滤+引用过滤+空节点过滤+util目录); (4) 基础设施测试 — jwt.strategy.spec.ts(3: 有效用户/不存在用户/禁用用户), all-exceptions.filter.spec.ts(4: HttpException/对象响应/未知错误/非Error异常) | backend/src/(26个新spec文件) | plan_8 |
| 2026-03-23 | **WP-08 阶段 1.3-1.5 & 阶段 4-6 推进**: (1) **XML 骨架验证测试** — xml-backbone-validation.spec.ts(37 tests: index.xml DTD/XSL引用、cn-regional.xml Schema、STF DTD、index-md5.txt一致性、叶元素ID唯一性/格式、xlink:href路径正确性、checksum验证); (2) **高级场景测试** — ectd-advanced-scenarios.spec.ts(24 tests: 撤回序列4步流程、格式转换cnsqt4、文件复用跨序列引用、骨架属性更新2.3.S/3.2.S重建、xml:lang外文处理、扩展节点3.2.R、空章节过滤、200+叶元素); (3) **集成工作流测试** — integration-workflow.spec.ts(24 tests: 完整多模块工作流验证、链式首次→后续提交、多违规检测、STF验证、角色审批流DRAFT→SUBMITTED→REJECTED→APPROVED、编辑锁并发/刷新/释放/强制解锁/心跳、序列号生成、并行冲突检测); (4) **验证引擎优化** — validator.service.ts 同步规则先跑+异步规则Promise.all并行(validateIchBackbone/validateCompleteness独立DB查询); (5) **数据库连接池** — prisma.service.ts 环境变量DB_CONNECTION_LIMIT/DB_POOL_TIMEOUT动态注入DATABASE_URL; (6) **Redis生产配置** — docker-compose.prod.yml Redis maxmemory 256mb+LRU淘汰+AOF+RDB; (7) **SSL/TLS** — nginx.conf HTTPS server block+HSTS+HTTP→HTTPS重定向(注释待启用), docker-compose.prod.yml 443端口+SSL卷挂载; (8) **安全头** — X-Frame-Options/X-Content-Type-Options/X-XSS-Protection/Referrer-Policy; (9) **备份策略** — scripts/backup-db.sh 自动化pg_dump+gzip+保留天数; (10) **util文件准备** — scripts/prepare-util-files.sh 从reference复制DTD/Schema/XSL/valid-values到backend/ectd-util; (11) **.env.production.example补全** — DB连接池/Redis内存/HTTPS端口/CORS/SSL路径/日志/Swagger/备份配置; (12) **用户文档** — docs/user-guide.md(系统概述/快速开始/eCTD包制作/申请类型指南/审批流程/验证错误表/签章指南); (13) **部署文档** — docs/deployment.md(系统要求/Docker部署/HTTPS/备份恢复/数据库维护/监控日志/升级/故障排除); **总计 720 tests / 47 suites 全部通过** | backend/src/ectd/services/(3个新spec), backend/src/ectd/services/validator.service.ts, backend/src/prisma/prisma.service.ts, docker-compose.prod.yml, frontend/nginx.conf, .env.production.example, scripts/(2个新脚本), docs/user-guide.md, docs/deployment.md | plan_8 |
| 2026-03-23 | **WP-08 CDE 验证规则全面对齐**: ValidatorService 完全重写，规则编码与 CDE eCTD验证标准V1.1 全部112条规则逐条对齐。(1) **规则编码重映射** — 3.5→3.10(首序列全NEW), 3.8→3.11(被修改文件存在性), 3.16→3.8(DELETE无文件), 3.4→3.7(非DELETE必须有文件), 3.9/3.10→3.12(路径规范), 3.22→3.23(APPEND警告), 4.1.4→4.1.11(M1首序列NEW), 4.2.2→4.2.1(申请编号), 6.18→6.16(PDF版本), 6.1→6.23(书签>5页), 6.24→6.17(附件), 6.21→6.10(外部链接); (2) **严重级别修正** — 2.4文件扩展名WARNING→ERROR, 6.16 PDF版本ERROR→WARNING, 6.24 JS/多媒体ERROR→WARNING, 6.8书签Zoom ERROR→WARNING; (3) **新增~30条规则** — Cat2: 2.1空文件夹, 2.9序列文件夹4位, 2.10序列编号连续性; Cat3: 3.5 modified-file重复检测, 3.16扩展节点限制(仅cnprt2), 3.18叶标题非空, 3.20叶标题首尾空格, 3.25-3.27骨架属性(indication/manufacturer/substance)非空, 3.28属性值首尾空格, 3.29跨申请引用检测, 3.30-3.34生命周期无效检测(增补/删除/替换分支), 3.36替换语言一致性; Cat4.1: 4.1.8/4.1.9文件引用, 4.1.14说明函必须NEW, 4.1.15首次提交申请表NEW, 4.1.16区域不允许扩展节点, 4.1.17/4.1.19叶标题检查, 4.1.20 cn-元素必须有叶, 4.1.24跨申请引用, 4.1.25-4.1.29生命周期, 4.1.30 append警告, 4.1.31语言一致; Cat4.2: 4.2.5相关序列, 4.2.9描述长度120字符, 4.2.10 depend-apt-rat-sqt, 4.2.11/4.2.12相关序列值, 4.2.13信封不可变, 4.2.14 RA类型不可变; Cat5: 5.5/5.6/5.7/5.8/5.14/5.15/5.16/5.17; Cat6: 6.1 PDF可读, 6.25内容可搜索; (4) **测试全面更新** — validator.service.spec.ts(63 tests), ectd-compliance.spec.ts, integration-workflow.spec.ts, ectd-advanced-scenarios.spec.ts 规则编码同步更新 | backend/src/ectd/services/validator.service.ts, backend/src/ectd/services/validator.service.spec.ts, backend/src/ectd/services/ectd-compliance.spec.ts, backend/src/ectd/services/integration-workflow.spec.ts, backend/src/ectd/services/ectd-advanced-scenarios.spec.ts | plan_8 |
| 2026-03-23 | **WP-08 最终推进**: (1) **前端组件测试** — CTDTree.test.tsx(14 tests: 基本渲染/状态Tag/必填标记/操作Tag/审批图标/节点选择/嵌套子节点/搜索/扩展节点), FilePanel.test.tsx(7 tests: 非叶提示/上传区/引用按钮/文件加载/空状态/引用Tag), useCVStore.test.ts(8 tests: 初始状态/fetchAll/getCvLabel跨类型/未知code回退); (2) **PDFExportService 测试** — pdf-export.service.spec.ts(12 tests: stripExternalLinks含http/mailto/多链接/内部链接保留/纯文本, exportToPDF含Puppeteer mock/未初始化/headerText/page.close, lifecycle含launch/close); (3) **Winston 日志** — main.ts 集成 nest-winston, 开发环境彩色格式+生产环境JSON, 文件轮转(error.log 10MB×5 + combined.log 10MB×10), LOG_LEVEL 环境变量可配; (4) **eCTD 流式打包** — PackageAssemblerService.assemblePackageStream 使用 PassThrough 流, 从MinIO直接流式读取(getFileStream), 控制器直接 stream.pipe(res), 压缩级别6平衡速度/体积; (5) **前端测试基础设施** — test/setup.ts 新增 window.matchMedia mock 解决 Ant Design 兼容性; **后端 732 tests / 48 suites + 前端 32 tests / 4 suites 全部通过** | frontend/src/components/(2个新test), frontend/src/stores/useCVStore.test.ts, frontend/src/test/setup.ts, backend/src/export/pdf-export.service.spec.ts, backend/src/main.ts, backend/src/ectd/services/package-assembler.service.ts, backend/src/ectd/ectd.controller.ts | plan_8 |
| 2026-03-23 | **Plan 2/3/4/7/8 剩余项完成**: (1) **RichEditor 组件测试** — RichEditor.test.tsx 18 tests: 基本渲染/工具栏/只读模式/字符统计/CSS eCTD 合规(宋体/黑体/字号/行距/颜色/Times New Roman); (2) **@mention 评论功能** — Comment.mentions JSON 字段 + Prisma migration + CreateCommentDto mentions[] + CommentService extractMentions + UserController GET /search (全用户可用) + UserService.searchUsers + 前端 userApi + PropertiesPanel @触发下拉选择/键盘导航/用户搜索/mention高亮渲染; (3) **交叉引用** — CrossReferenceExtension TipTap Mark(targetSection/targetTitle属性) + Toolbar LinkOutlined按钮 + RichEditor 章节树选择 Modal(TreeSelect) + 自动插入"见 X.X.X 章节名" + editor.css .cross-reference 样式; (4) **分片上传** — FileController POST /chunk endpoint(10MB/片) + FileService.handleChunk 内存分片存储+自动组装+调用uploadFile; (5) **Puppeteer 页面池** — PDFExportService acquirePage/releasePage 模式(PUPPETEER_POOL_SIZE 环境变量, 默认3), 页面复用避免频繁创建; (6) **React Query** — @tanstack/react-query + QueryClientProvider(5min staleTime); (7) **CTD 树虚拟滚动** — Ant Design Tree virtual + height 属性 | frontend/src/components/RichEditor/(3文件), backend/prisma/schema.prisma, backend/src/comment/, backend/src/user/, backend/src/file/, backend/src/export/pdf-export.service.ts, frontend/src/services/user.ts, frontend/src/pages/editor/PropertiesPanel.tsx, frontend/src/App.tsx, frontend/src/components/CTDTree.tsx | plan_2,3,4,7,8 |
| 2026-03-23 | **WP-08 CDE 验证规则最终对齐完成**: (1) ValidatorService 新增规则 5.4(研究标题不能为空) + 4.2.4 增强(原始编号10位数字格式校验); (2) ectd-compliance.spec.ts 全部规则编码修正完成 — 3.8→3.11(前序存在性), 3.16→3.8(DELETE无文件), 3.4→3.7(非DELETE需文件), 3.9/3.10→3.12(路径), 6.20→6.24(JS), 6.1→6.23(书签), 6.21→6.10(外部链接), 6.22→6.24(多媒体), 6.18→6.16(PDF版本), 6.24→6.17(附件), 6.25→6.26(字体), 5.5→5.6(studyId), 5.17→5.14(非STF位置), 3.22→3.23(APPEND警告), 2.4严重级别WARNING→ERROR; (3) **全部测试通过: 742 tests / 48 suites** | backend/src/ectd/services/validator.service.ts, backend/src/ectd/services/ectd-compliance.spec.ts | plan_8 |
| 2026-03-23 | **WP-08 最终完成: TipTap性能优化 + E2E测试覆盖**: (1) **TipTap大文档性能优化** — Toolbar组件 React.memo + useMemo(tableMenuItems), RichEditor useMemo(extensions/EditorContent), 内容同步优化(ref跟踪避免JSON.stringify比较), 交叉引用树数据缓存(treeDataCacheRef); (2) **E2E测试全面覆盖** — full-workflow.spec.ts 重写为4大场景14个测试: Scenario 1(登录→创建项目→创建申请CV级联→创建序列4 tests), Scenario 2(目录初始化→编辑文档2 tests), Scenario 3(验证→导出2 tests), Scenario 4(审批流程2 tests), Core Smoke Tests(Dashboard/ProjectList/Navigation/Logout 4 tests); (3) **Plan 8验收标准全部达成** — CDE验证112条规则对齐, 申请类型×注册行为组合测试通过, E2E覆盖核心工作流; **前端 50 tests / 5 suites 全部通过** | frontend/src/components/RichEditor/(index.tsx+Toolbar.tsx), frontend/e2e/full-workflow.spec.ts, docs/plans/plan_8.md | plan_8 |
| 2026-03-26 | **全面质量审计与 Bug 修复**: (1) **误导性 UI 全面修复** — CompletenessPanel: 总结统计区 requiredSections=0 时从"0/0"改为"无"/"无必填要求"/0% N/A 圆环; 模块 2-5 卡片从空进度条改为"无必填规则"提示+Tooltip; EditorPage: 完整性 Tag 从"必填 0/0"改为"无必填"; (2) **页面加载体验** — ProjectDetailPage+ApplicationDetailPage 新增 loading 状态+Spin 加载动画+Result 404 错误页面(替代空白页); (3) **表单状态修复** — ProjectDetailPage/ApplicationDetailPage 所有 Modal 的 onCancel 添加 form.resetFields()，防止关闭后残留旧数据; (4) **导出状态修复** — ExportService.exportPdfSingle: PDF 合规检查不通过时 status 从误导的 'completed' 改为 'compliance_warning'; ExportResult 接口新增该状态; (5) **后端测试修复** — 修复 31 个 XML 服务测试(index-xml/cn-regional-xml/xml-backbone-validation/ectd-advanced-scenarios: 补充 prisma.sequence.findUnique/findMany mock); 修复 5 个 PDFExportService 测试(移除已删除的 onModuleInit 调用); 修复 1 个 export.service 测试(匹配新 compliance_warning 状态); **742/742 后端测试 + 49/49 前端测试全部通过** | frontend/src/components/CompletenessPanel.tsx, frontend/src/pages/(editor/EditorPage+project/ProjectDetailPage+application/ApplicationDetailPage), backend/src/export/export.service.ts, backend/src/ectd/services/(5个spec文件), backend/src/export/(2个spec文件) | plan_8, UI/UX |
| 2026-03-26 | **eCTD 规范合规修复 + 导航 Bug 修复**: (1) **modified-file 属性生成** — IndexXmlService 和 CnRegionalXmlService 修复 CRITICAL 合规缺陷: REPLACE/DELETE/APPEND 操作的叶元素现在正确生成 modified-file 属性(ICH eCTD Specification V3.2.2 要求); Md5Service 新增 generateDeterministicLeafId() 方法(基于 sequenceId+nodeId+fileIndex 生成确定性叶 ID，使 modified-file 能正确引用前序序列的叶 ID); 两个 XML 服务均新增 buildPriorLeafIdMap() 方法查找前序序列叶节点; (2) **导航逻辑修复** — SequenceDetailPage 返回按钮从 `navigate(-1)` (浏览器历史) 改为显式父级路由 `navigate(/projects/${projectId}/applications/${appId})`，解决从编辑器返回序列详情后再点返回会回到编辑器的 Bug; (3) **面包屑交互化** — SequenceDetailPage 面包屑 4 级(项目列表→项目→申请→序列)均改为可点击 Link; EditorPage 面包屑同样改为可点击 Link(项目→申请→序列); (4) **内容完整性面板优化** — CompletenessPanel 模块 2-5 无必填规则时显示"无必填规则"提示 + Tooltip 解释(eCTD 验证标准 V1.1 第 4.3 节仅定义模块一必填规则)，替代易误解的"已完成 0/0" | backend/src/ectd/services/(index-xml+cn-regional-xml+md5), frontend/src/pages/(sequence/SequenceDetailPage+editor/EditorPage), frontend/src/components/CompletenessPanel.tsx | plan_5, plan_8 |
| 2026-03-26 | **全页 UI/UX 优化**: (1) **EditorPage 布局** — 顶栏 48→56px 高度+阴影, 面包屑改为斜杠分隔的层级路径, 操作按钮用竖线分组(状态/审批/工具), 面板折叠时显示带 Tooltip 的窄条(28px+hover 效果)替代固定 24px 灰条, 左侧目录 300→320px/右侧属性 320→360px 增加内容空间, 中心卡片带圆角+白色背景+章节标题头部栏; (2) **CTDTree** — 搜索框改小号+占位文案优化, 节点标题重排(monospace编号+正常标题 inline-flex), 状态/必填/完成用 6px 圆点(title属性)替代文字 Tag 减少视觉杂乱, 审批图标改用 Filled 版本更醒目, 移除操作类型 Tag(中心面板已展示), ResizeObserver 动态计算虚拟滚动高度, 新增 selectedNodeId prop 高亮当前节点; (3) **FilePanel** — 为中心宽面板重新设计: Upload.Dragger 增大(CloudUploadOutlined 36px+描述文案+格式限制提示), 文件列表从 List 改 Table(文件列含图标+名称+大小, 合规列含状态 Tag 可点查看详情, 操作列含预览/下载/删除), 空状态使用 Empty 组件+引导文案, 已上传数量标题栏+引用按钮放在同行, 合规 Modal 增加文件信息头部卡片; (4) **PropertiesPanel** — 移除固定"属性面板"标题(EditorPage 已有), Tabs 改用图标+文字标签(信息/版本/审批/评论), 信息 Tab: 模块/状态/操作三栏摘要卡片+全宽 Select(移除 size=small), 骨架属性包裹在蓝色背景卡片中, 统一使用 Field 组件(label+required 标记); 审批 Tab: 状态卡片带颜色背景+边框(绿/蓝/红/灰)+大号图标, 通过/驳回按钮等宽排列; 评论 Tab: 输入框增大 rows=3+圆角, @mention 下拉增加 Avatar+过渡动画, 评论气泡用 Avatar+左对齐布局+回复缩进 24px; (5) **测试修复** — CTDTree.test: ResizeObserver polyfill+图标 Filled 命名+圆点断言改 getByTitle+去除操作 Tag 断言+分离编号/标题匹配; FilePanel.test: 文案更新(拖拽/引用/空状态)+Table 渲染 timeout 15s; setup.ts: 全局 ResizeObserver mock; **49 tests / 5 suites 全部通过** | frontend/src/pages/editor/(EditorPage+PropertiesPanel), frontend/src/components/(CTDTree+FilePanel), frontend/src/test/setup.ts, frontend/src/components/(CTDTree.test+FilePanel.test) | UI/UX 优化 |
| 2026-03-26 | **编辑器页面重构: 在线编辑→文件上传模式**: EditorPage 移除 TipTap 富文本编辑器、自动保存(useAutoSave)、编辑锁(editLock)、文档加载(documentApi)，中心区域替换为 FilePanel 文件上传管理界面。用户在左侧 CTD 目录树选择叶节点后，直接在中心区域上传已编辑好的文件(PDF/XML/XPT/TXT/XSL)，右侧属性面板保留基本信息/版本/审批/评论 Tab(移除文件 Tab 避免重复)。保留审批提交、eCTD 验证、导出功能。顶栏移除保存状态指示器和手动保存按钮。 | frontend/src/pages/editor/EditorPage.tsx, frontend/src/pages/editor/PropertiesPanel.tsx | 功能调整 |
| 2026-03-22 | **WP-08 eCTD 合规测试、CDE 验证对齐与部署**: (1) **后端单元测试 9 套件 193 tests** — file-name-normalizer.service.spec.ts(44 tests: normalizeFileName/validateExtension/validateFileSize/buildEctdRelativePath/buildStoragePath/getSectionFolder/isCompliantFileName/validatePathSeparators), md5.service.spec.ts(13: calculateMd5/calculateMd5String/generateIndexMd5/generateLeafId), lifecycle.service.spec.ts(29: validateOperation 15条状态转换/validateReplaceLanguage/generateWithdrawOperations 4步撤回/checkParallelConflicts), validator.service.spec.ts(53: 6大类验证规则1.x-6.x逐条测试), cn-regional-xml.service.spec.ts(XML结构/信封/空章节/delete/转义), index-xml.service.spec.ts(DTD-XSL引用/模块2-5叶元素/模块1过滤/骨架属性/空章节/delete/node-extension), stf.service.spec.ts(generateStfXml/saveStf/getStf), application.service.spec.ts(create含前缀规则/findAll/findOne/remove), sequence.service.spec.ts(create自动递增/findOne/update/remove); (2) **后端 E2E 测试** — app.e2e-spec.ts 重写覆盖 auth flow/project CRUD/application/regulatory-activity/sequence/CV/health-check; (3) **前端测试基础设施** — 安装 vitest+@testing-library/react+jest-dom+user-event+jsdom+@playwright/test, vite.config.ts 添加 vitest 配置(globals/jsdom/setup), test/setup.ts(@testing-library/jest-dom), ProtectedRoute.test.tsx(3 tests: 认证渲染/未认证重定向/加载Spinner); (4) **前端 E2E** — playwright.config.ts(chromium/dev server/trace), e2e/login.spec.ts(3 tests: 登录表单/未认证重定向/空提交验证); (5) **性能优化** — ctd-template.service.ts getTemplateTree 7层嵌套include→单次flat查询+内存树构建, initializeSequenceNodes ~458次DB create→crypto.randomUUID()预生成+$transaction批量插入, App.tsx React.lazy()路由级代码分割+Suspense; (6) **生产部署** — backend/Dockerfile 多阶段构建(builder→production, Puppeteer系统依赖+中文字体+非root用户+HEALTHCHECK), frontend/Dockerfile 多阶段(node builder→nginx:1.27-alpine), frontend/nginx.conf(API反向代理/200MB upload/SPA fallback/gzip/静态缓存), docker-compose.prod.yml(postgres+redis+minio+backend+frontend, 健康检查+depends_on conditions), .env.production.example; (7) **健康检查** — @nestjs/terminus HealthModule+HealthController(数据库连通性检查); (8) **Swagger/OpenAPI** — main.ts 添加全局API前缀+CORS配置+Swagger文档(全部Tag)+环境日志级别; (9) **CI/CD** — .github/workflows/ci.yml GitHub Actions(4 jobs: backend-test含postgres+redis services/backend-build/frontend-build/docker-build); (10) **生产种子** — prisma/seed-production.ts 创建 admin 用户; (11) **PrismaService 增强** — datasource配置+环境日志级别+生命周期日志 | backend/src/(9个spec文件), backend/test/app.e2e-spec.ts, backend/Dockerfile, backend/src/health/, backend/src/main.ts, backend/prisma/seed-production.ts, frontend/src/test/, frontend/src/components/ProtectedRoute.test.tsx, frontend/playwright.config.ts, frontend/e2e/, frontend/Dockerfile, frontend/nginx.conf, frontend/src/App.tsx, docker-compose.prod.yml, .env.production.example, .github/workflows/ci.yml | plan_8 |
| 2026-03-26 | **前端 UI 误导修复**: (1) **CompletenessPanel** — requiredSections=0 时: 必填章节显示"无"而非"0个", 已完成必填显示"无必填要求"而非"0/0", 完成度圆形进度条显示"N/A"而非 100% 绿色; (2) **EditorPage** — 必填 Tag: requiredSections=0 时显示"无必填"替代"必填 0/0"; (3) **ProjectDetailPage** — 新增 loading 状态(Spin), 项目不存在时显示 Result 404 替代空白页, Modal onCancel 时调用 form.resetFields(); (4) **ApplicationDetailPage** — 新增 loading 状态(Spin), 申请不存在时显示 Result 404 替代空白页, RA/Sequence Modal onCancel 时调用 form.resetFields(); **TypeScript 编译通过** | frontend/src/components/CompletenessPanel.tsx, frontend/src/pages/editor/EditorPage.tsx, frontend/src/pages/project/ProjectDetailPage.tsx, frontend/src/pages/application/ApplicationDetailPage.tsx | UI/UX 修复 |
| 2026-04-24 | **fix(start.command): P0-8 + P0-9 客户冷装 v0.3.0 二次踩坑修复**：(1) **P0-8 .env 路径含空格未加引号**：`scripts/build-mac.sh` 输出的 `.env.template` `DATA_DIR=/Users/.../Application Support/eCTDTool` 没有引号 → Start.command 里 `set -a; source "$ENV_FILE"` 把空格后半段 `Support/eCTDTool` 当命令找不到 → set -e 退出。修：模板加双引号 `DATA_DIR="/Users/REPLACE_ME/Library/Application Support/eCTDTool"`，sed 替换的是引号内的占位符路径，引号保留。(2) **P0-9 reference 拷贝判断被 docker bind mount 副作用骗过**：原 Start.command 用 `[[ ! -d "$DATA_ROOT/reference" ]]` 判断"是否需要拷贝"，但 P0-8 失败那次 docker compose 已经被某条路径触发，bind mount `${DATA_DIR}/reference:/reference:ro` 在 host 路径不存在时**自动创建空目录** → 第二次 Start.command 重跑时判断"已存在 → 跳过 cp" → 挂载空目录给 backend → CV `ENOENT: cv-application-type.xml`。修：判断条件改为关键文件 sentinel `cv-application-type.xml` 是否存在，被空目录骗不过。两条 bug 均追加到 §1.9 E 段对照表（坑 #9 / 坑 #10） | scripts/build-mac.sh, docs/plans/software_upgrade.md | software_upgrade（P0-8/P0-9） |
| 2026-04-24 | **software_upgrade P0-2~P0-7 全部清零（首次冷装阻塞项一次性解决）**: (1) **P0-2 + P0-3 entrypoint** — 新增 `backend/docker-entrypoint.sh`，启动顺序：`prisma migrate deploy` → `node dist/prisma/seed.js`（用户全 upsert 幂等）→ `node dist/prisma/seed-ctd.js`（自带 `existing > 0` 守卫幂等）→ `exec node dist/src/main.js`；`backend/Dockerfile` 改 `ENTRYPOINT ["/app/docker-entrypoint.sh"]`，CMD 保留供 entrypoint exec。**采用幂等 seed 而非标记文件**（"干净一点"方案），每次启动都跑一遍是安全的。(2) **P0-4 reference 打包** — `scripts/build-mac.sh` 把 `reference/eCTD技术规范V1.1附件包/` + CTD 对应表 xlsx 打入产物（仅 ~1MB，不含 PDF 规范）；产物模板 `Start.command` 首启 `cp -R "$BASE_DIR/reference/"* "$DATA_DIR/reference/"`。(3) **P0-5 双架构** — `build-mac.sh` 加 `--arch arm64|amd64|both` 参数，默认 arm64（Apple 新机原生），包名加架构后缀；本机 docker 无 buildx，真双架构需在装了 buildx 的机器上预 load 后跑 build。(4) **P0-6 backend 漏传 redis password** — 真根因不是 env 传递问题。`backend/src/common/redis-cache.service.ts` 的 `new Redis({...})` 漏传 `password` 字段（`app.module.ts` 里 BullModule 是传了的）。补上 `password: process.env.REDIS_PASSWORD || undefined`。(5) **P0-7 healthcheck 路径 bug 新发现并修复** — `main.ts` 注释明确"No global prefix"，实际健康端点是 `/health`，但 `backend/Dockerfile`/`docker-compose.desktop.yml`/`docker-compose.prod.yml`/`scripts/build-mac.sh` 全部打到 `/api/health`，导致 backend 永远 unhealthy → frontend `dependency failed to start`。统一改为 `/health`。这是 B3 复验时连带发现的另一条隐藏冷装根因。(6) **build-mac.sh 加固** — 新增 5 个镜像 `docker image inspect` 预检（A5）、SHA256 自动产出（B5）、`Start.command` 加 `set -a; source; set +a` + 显式 `--env-file` 防止 env 传递行为差异、端口占用 lsof 自适应递增。(7) **`docker-compose.desktop.yml`** — 去掉 `version: '3.8'` 顶层属性（compose v2 已废弃；A1）。(8) **§1.9 检查表** — A 段 9 项（新增 A9 防 healthcheck 路径回归）、B 段 6 项全部完成（B1 双架构受本机 buildx 缺失限制为 amd64 单架构通过）；E 段对照表追加坑 #7 根因更正 + 坑 #8 healthcheck 路径。**B3 实测**：在 `/tmp/ectd-desktop-test` 起完整 stack，5 容器全 healthy，`http://localhost:18080/` 200，CV 5 个 XML 解析成功，entrypoint 日志看到 migrate+seed+exec 完整流程；REDIS_PASSWORD 非空场景下 backend 日志无 NOAUTH | backend/docker-entrypoint.sh, backend/Dockerfile, backend/src/common/redis-cache.service.ts, docker-compose.desktop.yml, docker-compose.prod.yml, scripts/build-mac.sh, docs/plans/software_upgrade.md | software_upgrade（P0 清零 + Release Gate A/B 段验证） |
